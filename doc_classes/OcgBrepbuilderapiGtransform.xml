<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBrepbuilderapiGtransform" inherits="RefCounted">
    <brief_description>Geometric transformation on a shape. The transformation to be applied is defined as a gp_GTrsf transformation. It may be: - a transformation equivalent to a gp_Trsf transformation, the most common case: you should , however, use a BRepAPI_Transform object to perform this kind of transformation; or - an affinity, or - more generally, any type of point transformation which may be defined by a three row, four column matrix of transformation. In the last two cases, the underlying geometry of the following shapes may change: - a curve which supports an edge of the shape, or - a surface which supports a face of the shape; For example, a circle may be transformed into an ellipse when applying an affinity transformation. The transformation is applied to: - all the curves which support edges of the shape, and - all the surfaces which support faces of the shape. A GTransform object provides a framework for: - defining the geometric transformation to be applied, - implementing the transformation algorithm, and - consulting the result.</brief_description>
    <description>! Geometric transformation on a shape.
! The transformation to be applied is defined as a gp_GTrsf
! transformation. It may be:
! -      a transformation equivalent to a gp_Trsf transformation, the
! most common case: you should , however, use a BRepAPI_Transform
! object to perform this kind of transformation; or
! -      an affinity, or
! -      more generally, any type of point transformation which may
! be defined by a three row, four column matrix of transformation.
! In the last two cases, the underlying geometry of the
! following shapes may change:
! -      a curve which supports an edge of the shape, or
! -      a surface which supports a face of the shape;
! For example, a circle may be transformed into an ellipse when
! applying an affinity transformation.
! The transformation is applied to:
! -      all the curves which support edges of the shape, and
! -      all the surfaces which support faces of the shape.
! A GTransform object provides a framework for:
! -      defining the geometric transformation to be applied,
! -      implementing the transformation algorithm, and
! -      consulting the result.</description>
    <methods>
        <method name="from_1" qualifiers="static">
            <description>Constructs a framework for applying the geometric transformation T to a shape. Use the function Perform to define the shape to transform.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Constructs a framework for applying the geometric transformation T to a shape, and applies it to the shape S. - If the transformation T is direct and isometric (i.e. if the determinant of the vectorial part of T is equal to 1.), and if Copy equals false (default value), the resulting shape is the same as the original but with a new location assigned to it. - In all other cases, the transformation is applied to a duplicate of S. Use the function Shape to access the result. Note: the constructed framework can be reused to apply the same geometric transformation to other shapes: just specify them with the function Perform.</description>
        </method>
        <method name="Perform">
            <description>Applies the geometric transformation defined at the time of construction of this framework to the shape S. - If the transformation T is direct and isometric (i.e. if the determinant of the vectorial part of T is equal to 1.), and if Copy equals false (default value), the resulting shape is the same as the original but with a new location assigned to it. - In all other cases, the transformation is applied to a duplicate of S. Use the function Shape to access the result. Note: this framework can be reused to apply the same geometric transformation to other shapes: just specify them by calling the function Perform again.</description>
        </method>
        <method name="Modified">
            <description>Returns the list of shapes modified from the shape &lt;S&gt;.</description>
        </method>
        <method name="ModifiedShape" qualifiers="const">
            <description>Returns the modified shape corresponding to &lt;S&gt;.</description>
        </method>
    </methods>
</class>