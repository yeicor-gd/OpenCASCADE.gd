<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBrepfilletapiMakechamfer" inherits="RefCounted">
    <brief_description>Describes functions to build chamfers on edges of a shell or solid. Chamfered Edge of a Shell or Solid A MakeChamfer object provides a framework for: - initializing the construction algorithm with a given shape, - acquiring the data characterizing the chamfers, - building the chamfers and constructing the resulting shape, and - consulting the result.</brief_description>
    <description>! Describes functions to build chamfers on edges of a shell or solid.
! Chamfered Edge of a Shell or Solid
! A MakeChamfer object provides a framework for:
! -   initializing the construction algorithm with a given shape,
! -   acquiring the data characterizing the chamfers,
! -   building the chamfers and constructing the resulting shape, and
! -   consulting the result.</description>
    <methods>
        <method name="BRepFilletAPI_MakeChamfer" qualifiers="static">
            <description>Initializes an algorithm for computing chamfers on the shape S. The edges on which chamfers are built are defined using the Add function.</description>
        </method>
        <method name="Add_1">
            <description>Adds edge E to the table of edges used by this algorithm to build chamfers, where the parameters of the chamfer must be set after the</description>
        </method>
        <method name="Add_2">
            <description>Adds edge E to the table of edges used by this algorithm to build chamfers, where the parameters of the chamfer are given by the distance Dis (symmetric chamfer). The Add function results in a contour being built by propagation from the edge E (i.e. the contour contains at least this edge). This contour is composed of edges of the shape which are tangential to one another and which delimit two series of tangential faces, with one series of faces being located on either side of the contour. Warning Nothing is done if edge E or the face F does not belong to the initial shape.</description>
        </method>
        <method name="SetDist">
            <description>Sets the distances Dis1 and Dis2 which give the parameters of the chamfer along the contour of index IC generated using the Add function in the internal data structure of this algorithm. The face F identifies the side where Dis1 is measured. Warning Nothing is done if either the edge E or the face F does not belong to the initial shape.</description>
        </method>
        <method name="Add_3">
            <description>Adds edge E to the table of edges used by this algorithm to build chamfers, where the parameters of the chamfer are given by the two distances Dis1 and Dis2; the face F identifies the side where Dis1 is measured. The Add function results in a contour being built by propagation from the edge E (i.e. the contour contains at least this edge). This contour is composed of edges of the shape which are tangential to one another and which delimit two series of tangential faces, with one series of faces being located on either side of the contour. Warning Nothing is done if edge E or the face F does not belong to the initial shape.</description>
        </method>
        <method name="SetDists">
            <description>Sets the distances Dis1 and Dis2 which give the parameters of the chamfer along the contour of index IC generated using the Add function in the internal data structure of this algorithm. The face F identifies the side where Dis1 is measured. Warning Nothing is done if either the edge E or the face F does not belong to the initial shape.</description>
        </method>
        <method name="Dists" qualifiers="const">
            <description>Returns the distances Dis1 and Dis2 which give the parameters of the chamfer along the contour of index IC in the internal data structure of this algorithm. Warning -1. is returned if IC is outside the bounds of the table of contours.</description>
        </method>
        <method name="AddDA">
            <description>Adds a fillet contour in the builder (builds a contour of tangent edges to &lt;E&gt; and sets the distance &lt;Dis1&gt; and angle &lt;Angle&gt; ( parameters of the chamfer ) ).</description>
        </method>
        <method name="SetDistAngle">
            <description>set the distance &lt;Dis&gt; and &lt;Angle&gt; of the fillet contour of index &lt;IC&gt; in the DS with &lt;Dis&gt; on &lt;F&gt;. if the face &lt;F&gt; is not one of common faces of an edge of the contour &lt;IC&gt;</description>
        </method>
        <method name="GetDistAngle" qualifiers="const">
            <description>gives the distances &lt;Dis&gt; and &lt;Angle&gt; of the fillet contour of index &lt;IC&gt; in the DS</description>
        </method>
        <method name="SetMode">
            <description>Sets the mode of chamfer</description>
        </method>
        <method name="IsSymetric" qualifiers="const">
            <description>return True if chamfer symmetric false else.</description>
        </method>
        <method name="IsTwoDistances" qualifiers="const">
            <description>return True if chamfer is made with two distances false else.</description>
        </method>
        <method name="IsDistanceAngle" qualifiers="const">
            <description>return True if chamfer is made with distance and angle false else.</description>
        </method>
        <method name="ResetContour">
            <description>Erases the chamfer parameters on the contour of index IC in the internal data structure of this algorithm. Use the SetDists function to reset this data. Warning Nothing is done if IC is outside the bounds of the table of contours.</description>
        </method>
        <method name="NbContours" qualifiers="const">
            <description>Returns the number of contours generated using the Add function in the internal data structure of this algorithm.</description>
        </method>
        <method name="Contour" qualifiers="const">
            <description>Returns the index of the contour in the internal data structure of this algorithm, which contains the edge E of the shape. This function returns 0 if the edge E does not belong to any contour. Warning This index can change if a contour is removed from the internal data structure of this algorithm using the function Remove.</description>
        </method>
        <method name="NbEdges" qualifiers="const">
            <description>Returns the number of edges in the contour of index I in the internal data structure of this algorithm. Warning Returns 0 if I is outside the bounds of the table of contours.</description>
        </method>
        <method name="Edge" qualifiers="const">
            <description>Returns the edge of index J in the contour of index I in the internal data structure of this algorithm. Warning Returns a null shape if: - I is outside the bounds of the table of contours, or - J is outside the bounds of the table of edges of the contour of index I.</description>
        </method>
        <method name="Remove">
            <description>Removes the contour in the internal data structure of this algorithm which contains the edge E of the shape. Warning Nothing is done if the edge E does not belong to the contour in the internal data structure of this algorithm.</description>
        </method>
        <method name="Length" qualifiers="const">
            <description>Returns the length of the contour of index IC in the internal data structure of this algorithm. Warning Returns -1. if IC is outside the bounds of the table of contours.</description>
        </method>
        <method name="FirstVertex" qualifiers="const">
            <description>Returns the first vertex of the contour of index IC in the internal data structure of this algorithm. Warning Returns a null shape if IC is outside the bounds of the table of contours.</description>
        </method>
        <method name="LastVertex" qualifiers="const">
            <description>Returns the last vertex of the contour of index IC in the internal data structure of this algorithm. Warning Returns a null shape if IC is outside the bounds of the table of contours.</description>
        </method>
        <method name="Abscissa" qualifiers="const">
            <description>Returns the curvilinear abscissa of the vertex V on the contour of index IC in the internal data structure of this algorithm. Warning Returns -1. if: - IC is outside the bounds of the table of contours, or - V is not on the contour of index IC.</description>
        </method>
        <method name="RelativeAbscissa" qualifiers="const">
            <description>Returns the relative curvilinear abscissa (i.e. between 0 and 1) of the vertex V on the contour of index IC in the internal data structure of this algorithm. Warning Returns -1. if: - IC is outside the bounds of the table of contours, or - V is not on the contour of index IC.</description>
        </method>
        <method name="ClosedAndTangent" qualifiers="const">
            <description>eturns true if the contour of index IC in the internal data structure of this algorithm is closed and tangential at the point of closure. Warning Returns false if IC is outside the bounds of the table of contours.</description>
        </method>
        <method name="Closed" qualifiers="const">
            <description>Returns true if the contour of index IC in the internal data structure of this algorithm is closed. Warning Returns false if IC is outside the bounds of the table of contours.</description>
        </method>
        <method name="Build">
            <description>Builds the chamfers on all the contours in the internal data structure of this algorithm and constructs the resulting shape. Use the function IsDone to verify that the chamfered shape is built. Use the function Shape to retrieve the chamfered shape. Warning The construction of chamfers implements highly complex construction algorithms. Consequently, there may be instances where the algorithm fails, for example if the data defining the parameters of the chamfer is not compatible with the geometry of the initial shape. There is no initial analysis of errors and these only become evident at the construction stage. Additionally, in the current software release, the following cases are not handled: - the end point of the contour is the point of intersection of 4 or more edges of the shape, or - the intersection of the chamfer with a face which limits the contour is not fully contained in this face.</description>
        </method>
        <method name="Builder" qualifiers="const">
            <description>Returns the internal filleting algorithm.</description>
        </method>
        <method name="Generated">
            <description>Returns the list of shapes generated from the shape &lt;EorV&gt;.</description>
        </method>
        <method name="Modified">
            <description>Returns the list of shapes modified from the shape &lt;F&gt;.</description>
        </method>
    </methods>
</class>