<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapeanalysisChecksmallface" inherits="RefCounted">
    <brief_description>Analysis of the face size</brief_description>
    <description>! Analysis of the face size</description>
    <methods>
        <method name="ShapeAnalysis_CheckSmallFace" qualifiers="static">
            <description>Creates an empty tool Checks a Shape i.e. each of its faces, records checks as diagnostics in the &lt;infos&gt;</description>
        </method>
        <method name="IsSpotFace" qualifiers="const">
            <description>Checks if a Face is as a Spot Returns 0 if not, 1 if yes, 2 if yes and all vertices are the same By default, considers the tolerance zone of its vertices A given value &lt;tol&gt; may be given to check a spot of this size If a Face is a Spot, its location is returned in &lt;spot&gt;, and &lt;spotol&gt; returns an equivalent tolerance, which is computed as half of max dimension of min-max box of the face</description>
        </method>
        <method name="CheckSpotFace">
            <description>Acts as IsSpotFace, but records in &lt;infos&gt; a diagnostic "SpotFace" with the Pnt as value (data "Location")</description>
        </method>
        <method name="IsStripSupport">
            <description>Checks if a Face lies on a Surface which is a strip So the Face is a strip. But a Face may be a strip elsewhere ..</description>
        </method>
        <method name="CheckStripEdges" qualifiers="const">
            <description>Checks if two edges define a strip, i.e. distance maxi below tolerance, given or some of those of E1 and E2</description>
        </method>
        <method name="FindStripEdges">
            <description>Searches for two and only two edges up tolerance Returns True if OK, false if not 2 edges If True, returns the two edges and their maximum distance</description>
        </method>
        <method name="CheckSingleStrip">
            <description>Checks if a Face is a single strip, i.e. brings two great edges which are confused on their whole length, possible other edges are small or null length</description>
        </method>
        <method name="CheckStripFace">
            <description>Checks if a Face is as a Strip Returns 0 if not or non determined, 1 if in U, 2 if in V By default, considers the tolerance zone of its edges A given value &lt;tol&gt; may be given to check a strip of max this width</description>
        </method>
        <method name="CheckSplittingVertices">
            <description>Checks if a Face brings vertices which split it, either confused with non adjacent vertices, or confused with their projection on non adjacent edges Returns the count of found splitting vertices Each vertex then brings a diagnostic "SplittingVertex", with data : "Face" for the face, "Edge" for the split edge</description>
        </method>
        <method name="CheckPin">
            <description>Checks if a Face has a pin, which can be edited No singularity : no pin, returns 0 If there is a pin, checked topics, with returned value : - 0 : nothing to do more - 1 : "smooth", i.e. not a really sharp pin -&gt; diagnostic "SmoothPin" - 2 : stretched pin, i.e. is possible to relimit the face by another vertex, so that this vertex still gives a pin -&gt; diagnostic "StretchedPin" with location of vertex (Pnt)</description>
        </method>
        <method name="CheckTwisted">
            <description>Checks if a Face is twisted (apart from checking Pin, i.e. it does not give information on pin, only "it is twisted")</description>
        </method>
        <method name="Status" qualifiers="const">
            <description>Returns the status of last call to Perform() ShapeExtend_OK : face was OK, nothing done ShapeExtend_DONE1: some wires are fixed ShapeExtend_DONE2: orientation of wires fixed ShapeExtend_DONE3: missing seam added ShapeExtend_DONE4: small area wire removed ShapeExtend_DONE5: natural bounds added ShapeExtend_FAIL1: some fails during fixing wires ShapeExtend_FAIL2: cannot fix orientation of wires ShapeExtend_FAIL3: cannot add missing seam ShapeExtend_FAIL4: cannot remove small area wire</description>
        </method>
        <method name="SetTolerance">
            <description>Sets a fixed Tolerance to check small face By default, local tolerance zone is considered Sets a fixed MaxTolerance to check small face Sets a fixed Tolerance to check small face By default, local tolerance zone is considered Unset fixed tolerance, comes back to local tolerance zones Unset fixed tolerance, comes back to local tolerance zones</description>
        </method>
        <method name="Tolerance" qualifiers="const">
            <description>Returns the tolerance to check small faces, negative value if local tolerances zones are to be considered</description>
        </method>
    </methods>
</class>