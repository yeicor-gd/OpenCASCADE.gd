<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBrepprimapiMakeprism" inherits="RefCounted">
    <brief_description>Describes functions to build linear swept topologies, called prisms. A prism is defined by: - a basis shape, which is swept, and - a sweeping direction, which is: - a vector for finite prisms, or - a direction for infinite or semi-infinite prisms. The basis shape must not contain any solids. The profile generates objects according to the following rules: - Vertices generate Edges - Edges generate Faces. - Wires generate Shells. - Faces generate Solids. - Shells generate Composite Solids A MakePrism object provides a framework for: - defining the construction of a prism, - implementing the construction algorithm, and - consulting the result.</brief_description>
    <description>! Describes functions to build linear swept topologies, called prisms.
! A prism is defined by:
! -   a basis shape, which is swept, and
! -   a sweeping direction, which is:
! -   a vector for finite prisms, or
! -   a direction for infinite or semi-infinite prisms.
! The basis shape must not contain any solids.
! The profile generates objects according to the following rules:
! -   Vertices generate Edges
! -   Edges generate Faces.
! -   Wires generate Shells.
! -   Faces generate Solids.
! -   Shells generate Composite Solids
! A MakePrism object provides a framework for:
! -   defining the construction of a prism,
! -   implementing the construction algorithm, and
! -   consulting the result.</description>
    <methods>
        <method name="from_1" qualifiers="static">
            <description>Builds the prism of base S and vector V. If C is true, S is copied. If Canonize is true then generated surfaces are attempted to be canonized in simple types</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Builds a semi-infinite or an infinite prism of base S. If Inf is true the prism is infinite, if Inf is false the prism is semi-infinite (in the direction D). If C is true S is copied (for semi-infinite prisms). If Canonize is true then generated surfaces are attempted to be canonized in simple types</description>
        </method>
        <method name="Build">
            <description>Builds the resulting shape (redefined from MakeShape).</description>
        </method>
        <method name="FirstShape_1">
            <description>Returns the TopoDS Shape of the bottom of the prism.</description>
        </method>
        <method name="LastShape_1">
            <description>Returns the TopoDS Shape of the top of the prism. In the case of a finite prism, FirstShape returns the basis of the prism, in other words, S if Copy is false; otherwise, the copy of S belonging to the prism. LastShape returns the copy of S translated by V at the time of construction.</description>
        </method>
        <method name="Generated">
            <description>Returns ListOfShape from TopTools.</description>
        </method>
        <method name="IsDeleted">
            <description>Returns true if the shape S has been deleted.</description>
        </method>
        <method name="FirstShape_2">
            <description>Returns the TopoDS Shape of the bottom of the prism. generated with theShape (subShape of the generating shape).</description>
        </method>
        <method name="LastShape_2">
            <description>Returns the TopoDS Shape of the top of the prism. generated with theShape (subShape of the generating shape).</description>
        </method>
    </methods>
</class>