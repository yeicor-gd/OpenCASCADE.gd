<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGceMakeparab" inherits="RefCounted">
    <brief_description>Implements construction algorithms for `gp_Parab`. The parabola is infinite in the parameter range ]-infinite, +infinite[. The vertex is the `Location` point of the local coordinate system.</brief_description>
    <description>! Implements construction algorithms for `gp_Parab`.
! The parabola is infinite in the parameter range ]-infinite, +infinite[.
! The vertex is the `Location` point of the local coordinate system.
!
! The `XDirection` and `YDirection` define the parabola plane.
!
! The `XAxis` (`Location`, `XDirection`) is the symmetry axis and is oriented
! from the vertex to the focus.
!
! The `YAxis` (`Location`, `YDirection`) is parallel to the directrix.
!
! The equation in the local coordinate system is:
! `Y**2 = (2*P) * X`, where `P` is the parameter
! (distance between focus and directrix).
! The focal length `F = P / 2` is the distance from vertex to focus.
!
! Supported constructions:
! - from local coordinate system and focal length;
! - from directrix and focus.</description>
    <methods>
        <method name="from_1" qualifiers="static">
            <description>Creates a parabola from local coordinate system and focal length.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Creates a parabola from directrix and focus.</description>
        </method>
        <method name="Value" qualifiers="const">
            <description>Returns the constructed parabola.</description>
        </method>
        <method name="Operator" qualifiers="const">
            <description>Alias for Value() returning a copy.</description>
        </method>
    </methods>
</class>