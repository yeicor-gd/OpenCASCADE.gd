<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTDFData" inherits="RefCounted">
    <brief_description>This class is used to manipulate a complete independent, self sufficient data structure and its services:</brief_description>
    <description>! This class is used to manipulate a complete independent,
! self sufficient data structure and its services:
!
! Access to the root label;
!
! Opens, aborts, commits a transaction;
!
! Generation and use of Delta, depending on the time.
! This class uses a special allocator
! (see LabelNodeAllocator() method)
! for more efficient allocation of objects in memory.</description>
    <methods>
        <method name="TDF_Data" qualifiers="static">
            <description>A new and empty Data structure.</description>
        </method>
        <method name="Root" qualifiers="const">
            <description>Returns the root label of the Data structure.</description>
        </method>
        <method name="Transaction" qualifiers="const">
            <description>Returns the current transaction number.</description>
        </method>
        <method name="Time" qualifiers="const">
            <description>Returns the current tick. It is incremented each Commit.</description>
        </method>
        <method name="IsApplicable" qualifiers="const">
            <description>Returns true if &lt;aDelta&gt; is applicable HERE and NOW.</description>
        </method>
        <method name="Undo">
            <description>Apply &lt;aDelta&gt; to undo a set of attribute modifications.</description>
        </method>
        <method name="NotUndoMode" qualifiers="const">
            <description>Returns the undo mode status.</description>
        </method>
        <method name="AllowModification">
            <description>Sets modification mode.</description>
        </method>
        <method name="IsModificationAllowed" qualifiers="const">
            <description>returns modification mode.</description>
        </method>
        <method name="SetAccessByEntries">
            <description>Initializes a mechanism for fast access to the labels by their entries. The fast access is useful for large documents and often access to the labels via entries. Internally, a table of entry - label is created, which allows to obtain a label by its entry in a very fast way. If the mechanism is turned off, the internal table is cleaned. New labels are added to the table, if the mechanism is on (no need to re-initialize the mechanism).</description>
        </method>
        <method name="IsAccessByEntries" qualifiers="const">
            <description>Returns a status of mechanism for fast access to the labels via entries.</description>
        </method>
        <method name="RegisterLabel">
            <description>An internal method. It is used internally on creation of new labels. It adds a new label into internal table for fast access to the labels by entry.</description>
        </method>
        <method name="LabelNodeAllocator" qualifiers="const">
            <description>Returns TDF_HAllocator, which is an incremental allocator used by TDF_LabelNode. This allocator is used to manage TDF_LabelNode objects, but it can also be used for allocating memory to application-specific data (be careful because this allocator does not release the memory). The benefits of this allocation scheme are noticeable when dealing with large OCAF documents, due to: 1. Very quick allocation of objects (memory heap is not used, the algorithm that replaces it is very simple). 2. Very quick destruction of objects (memory is released not by destructors of TDF_LabelNode, but rather by the destructor of TDF_Data). 3. TDF_LabelNode objects do not fragmentize the memory; they are kept compactly in a number of arrays of 16K each. 4. Swapping is reduced on large data, because each document now occupies a smaller number of memory pages.</description>
        </method>
        <method name="DumpJson" qualifiers="const">
            <description>Dumps the content of me into the stream</description>
        </method>
    </methods>
</class>