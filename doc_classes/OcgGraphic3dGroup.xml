<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGraphic3dGroup" inherits="RefCounted">
    <brief_description>This class allows the definition of groups of primitives inside of graphic objects (presentations). A group contains the primitives and attributes for which the range is limited to this group. The primitives of a group can be globally suppressed.</brief_description>
    <description>! This class allows the definition of groups
! of primitives inside of graphic objects (presentations).
! A group contains the primitives and attributes
! for which the range is limited to this group.
! The primitives of a group can be globally suppressed.
!
! There are two main group usage models:
!
! 1) Non-modifiable, or unbounded, group ('black box').
! Developers can repeat a sequence of
! SetPrimitivesAspect() with AddPrimitiveArray() methods arbitrary number of times
! to define arbitrary number of primitive "blocks" each having individual aspect values.
! Any modification of such a group is forbidden, as aspects and primitives are mixed
! in memory without any high-level logical structure, and any modification is very likely to
! result in corruption of the group internal data. It is necessary to recreate such a group as a
! whole when some attribute should be changed. (for example, in terms of AIS it is necessary to
! re-Compute() the whole presentation each time). 2) Bounded group. Developers should specify the
! necessary group aspects with help of SetGroupPrimitivesAspect() and then add primitives to the
! group. Such a group have simplified organization in memory (a single block of attributes
! followed by a block of primitives) and therefore it can be modified, if it is necessary to
! change parameters of some aspect that has already been set, using methods:
! IsGroupPrimitivesAspectSet() to detect which aspect was set for primitives;
! GroupPrimitivesAspect() to read current aspect values
! and SetGroupPrimitivesAspect() to set new values.
!
! Developers are strongly recommended to take all the above into account when filling
! Graphic3d_Group with aspects and primitives and choose the group usage model beforehand out of
! application needs. Note that some Graphic3d_Group class virtual methods contain only base
! implementation that is extended by the descendant class in OpenGl package.</description>
    <methods>
        <method name="Clear">
            <description>Suppress all primitives and attributes of &lt;me&gt;. To clear group without update in Graphic3d_StructureManager pass Standard_False as &lt;theUpdateStructureMgr&gt;. This used on context and viewer destruction, when the pointer to structure manager in Graphic3d_Structure could be already released (pointers are used here to avoid handle cross-reference);</description>
        </method>
        <method name="Remove">
            <description>Suppress the group &lt;me&gt; in the structure. Warning: No more graphic operations in &lt;me&gt; after this call. Modifies the current modelling transform persistence (pan, zoom or rotate) Get the current modelling transform persistence (pan, zoom or rotate)</description>
        </method>
        <method name="AddText">
            <description>Adds a text for display</description>
        </method>
        <method name="AddPrimitiveArray_2">
            <description>Adds an array of primitives for display</description>
        </method>
        <method name="AddPrimitiveArray_1">
            <description>Adds an array of primitives for display</description>
        </method>
        <method name="Transformation" qualifiers="const">
            <description>Return transformation.</description>
        </method>
        <method name="SetTransformation">
            <description>Assign transformation.</description>
        </method>
        <method name="TransformPersistence" qualifiers="const">
            <description>Return transformation persistence.</description>
        </method>
        <method name="SetTransformPersistence">
            <description>Set transformation persistence.</description>
        </method>
        <method name="IsDeleted" qualifiers="const">
            <description>Returns Standard_True if the group &lt;me&gt; is deleted. &lt;me&gt; is deleted after the call Remove (me) or the associated structure is deleted.</description>
        </method>
        <method name="IsEmpty" qualifiers="const">
            <description>Returns Standard_True if the group &lt;me&gt; is empty.</description>
        </method>
        <method name="MinMaxValues" qualifiers="const">
            <description>Returns the coordinates of the boundary box of the group.</description>
        </method>
        <method name="SetMinMaxValues">
            <description>Sets the coordinates of the boundary box of the group.</description>
        </method>
        <method name="Structure" qualifiers="const">
            <description>Returns the structure containing the group &lt;me&gt;.</description>
        </method>
        <method name="SetClosed">
            <description>Changes property shown that primitive arrays within this group form closed volume (do no contain open shells).</description>
        </method>
        <method name="IsClosed" qualifiers="const">
            <description>Return true if primitive arrays within this graphic group form closed volume (do no contain open shells).</description>
        </method>
        <method name="Marker">
            <description>! @name obsolete methods</description>
        </method>
        <method name="Text_3">
            <description>Creates the string &lt;AText&gt; at position &lt;APoint&gt;. The 3D point of attachment is projected. The text is written in the plane of projection. The attributes are given with respect to the plane of projection. AHeight : Height of text. (Relative to the Normalized Projection Coordinates (NPC) Space). AAngle : Orientation of the text (with respect to the horizontal).</description>
        </method>
        <method name="Text_1">
            <description>Creates the string &lt;AText&gt; at position &lt;APoint&gt;. The 3D point of attachment is projected. The text is written in the plane of projection. The attributes are given with respect to the plane of projection. AHeight : Height of text. (Relative to the Normalized Projection Coordinates (NPC) Space). The other attributes have the following default values: AAngle : PI / 2. ATp : TP_RIGHT AHta : HTA_LEFT AVta : VTA_BOTTOM</description>
        </method>
        <method name="Text_5">
            <description>Creates the string &lt;theText&gt; at orientation &lt;theOrientation&gt; in 3D space.</description>
        </method>
        <method name="DumpJson" qualifiers="const">
            <description>Dumps the content of me into the stream</description>
        </method>
    </methods>
</class>