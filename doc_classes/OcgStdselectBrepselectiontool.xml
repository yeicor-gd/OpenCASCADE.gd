<?xml version='1.0' encoding='utf-8'?>
<class name="OcgStdselectBrepselectiontool" inherits="RefCounted">
    <brief_description>Tool to create specific selections (sets of primitives) for Shapes from Topology. These Selections may be used in dynamic selection Mechanism Given a Shape and a mode of selection (selection of vertices, edges,faces ...) , This Tool Computes corresponding sensitive primitives, puts them in an entity called Selection (see package SelectMgr) and returns it.</brief_description>
    <description>! Tool to create specific selections (sets of primitives)
! for Shapes from Topology.
! These Selections may be used  in dynamic selection
! Mechanism
! Given a Shape and a mode of selection
! (selection of vertices,
! edges,faces ...) , This Tool Computes corresponding sensitive primitives,
! puts them in an entity called Selection (see package SelectMgr) and returns it.
!
! A Priority for the decomposed pickable objects can be given ;
! by default There is A Preset Hierarchy:
! Vertex             priority : 5
! Edge               priority : 4
! Wire               priority : 3
! Face               priority : 2
! Shell,solid,shape  priority : 1
! the default priority in the following methods has no sense - it's only taken in account
! when the user gives a value between 0 and 10.
! IMPORTANT : This decomposition creates BRepEntityOwner instances (from StdSelect).
! which are stored in the Sensitive Entities coming from The Decomposition.
!
! the result of picking in a ViewerSelector return EntityOwner from SelectMgr;
! to know what kind of object was picked :
!
! ENTITY_OWNER -&gt; Selectable() gives the selectableobject which
! was decomposed into pickable elements.
! Handle(StdSelect_BRepOwner)::DownCast(ENTITY_OWNER) -&gt; Shape()
! gives the real picked shape (edge,vertex,shape...)</description>
    <methods>
        <method name="Load_1" qualifiers="static">
            <description>Decomposition of &lt;aShape&gt; into sensitive entities following a mode of decomposition &lt;aType&gt;. These entities are stored in &lt;aSelection&gt;. BrepOwners are created to store the identity of the picked shapes during the selection process. In those BRepOwners is also stored the original shape. But One can't get the selectable object which was decomposed to give the sensitive entities. maximal parameter is used for infinite objects, to limit the sensitive Domain.... If AutoTriangulation = True, a Triangulation will be computed for faces which have no existing one. if AutoTriangulation = False the old algorithm will be called to compute sensitive entities on faces.</description>
        </method>
        <method name="Load_2" qualifiers="static">
            <description>Same functionalities ; the only difference is that the selectable object from which the selection comes is stored in each Sensitive EntityOwner; decomposition of &lt;aShape&gt; into sensitive entities following a mode of decomposition &lt;aType&gt;. These entities are stored in &lt;aSelection&gt; The Major difference is that the known users are first inserted in the BRepOwners. the original shape is the last user... (see EntityOwner from SelectBasics and BrepOwner)...</description>
        </method>
        <method name="GetStandardPriority" qualifiers="static">
            <description>Returns the standard priority of the shape aShap having the type aType. This priority is passed to a StdSelect_BRepOwner object. You can use the function Load to modify the selection priority of an owner to make one entity more selectable than another one.</description>
        </method>
        <method name="ComputeSensitive" qualifiers="static">
            <description>Computes the sensitive primitives, stores them in the SelectMgr_Selection object, and returns this object.</description>
        </method>
        <method name="GetSensitiveForCylinder" qualifiers="static">
            <description>Creates a sensitive cylinder.</description>
        </method>
        <method name="GetEdgeSensitive" qualifiers="static">
            <description>Create a sensitive edge or sensitive wire.</description>
        </method>
        <method name="PreBuildBVH" qualifiers="static">
            <description>Traverses the selection given and pre-builds BVH trees for heavyweight sensitive entities containing more than BVH_PRIMITIVE_LIMIT (defined in .cxx file) sub-elements.</description>
        </method>
    </methods>
</class>