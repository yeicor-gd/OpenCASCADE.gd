<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTdocstdMultitransactionmanager" inherits="RefCounted">
    <brief_description>Class for synchronization of transactions within multiple documents. Each transaction of this class involvess one transaction in each modified document.</brief_description>
    <description>! Class for synchronization of transactions within multiple documents.
! Each transaction of this class involvess one transaction in each modified document.
!
! The documents to be synchronized should be added explicitly to
! the manager; then its interface is used to ensure that all transactions
! (Open/Commit, Undo/Redo) are performed synchronously in all managed documents.
!
! The current implementation does not support nested transactions
! on multitransaction manager level. It only sets the flag enabling
! or disabling nested transactions in all its documents, so that
! a nested transaction can be opened for each particular document
! with TDocStd_Document class interface.
!
! NOTE: When you invoke CommitTransaction of multi transaction
! manager, all nested transaction of its documents will be closed (committed).</description>
    <methods>
        <method name="TDocStd_MultiTransactionManager" qualifiers="static">
            <description>Constructor</description>
        </method>
        <method name="SetUndoLimit">
            <description>Sets undo limit for the manager and all documents.</description>
        </method>
        <method name="GetUndoLimit" qualifiers="const">
            <description>Returns undo limit for the manager.</description>
        </method>
        <method name="Undo">
            <description>Undoes the current transaction of the manager. It calls the Undo () method of the document being on top of the manager list of undos (list.First()) and moves the list item to the top of the list of manager redos (list.Prepend(item)).</description>
        </method>
        <method name="Redo">
            <description>Redoes the current transaction of the application. It calls the Redo () method of the document being on top of the manager list of redos (list.First()) and moves the list item to the top of the list of manager undos (list.Prepend(item)).</description>
        </method>
        <method name="GetAvailableUndos" qualifiers="const">
            <description>Returns available manager undos.</description>
        </method>
        <method name="GetAvailableRedos" qualifiers="const">
            <description>Returns available manager redos.</description>
        </method>
        <method name="OpenCommand">
            <description>Opens transaction in each document and sets the flag that transaction is opened. If there are already opened transactions in the documents, these transactions will be aborted before opening new ones.</description>
        </method>
        <method name="AbortCommand">
            <description>Unsets the flag of started manager transaction and aborts transaction in each document.</description>
        </method>
        <method name="CommitCommand_1">
            <description>Commits transaction in all documents and fills the transaction manager with the documents that have been changed during the transaction. Returns True if new data has been added to myUndos. NOTE: All nested transactions in the documents will be committed.</description>
        </method>
        <method name="HasOpenCommand" qualifiers="const">
            <description>Returns true if a transaction is opened.</description>
        </method>
        <method name="RemoveLastUndo">
            <description>Removes undo information from the list of undos of the manager and all documents which have been modified during the transaction.</description>
        </method>
        <method name="DumpTransaction" qualifiers="const">
            <description>Dumps transactions in undos and redos</description>
        </method>
        <method name="AddDocument">
            <description>Adds the document to the transaction manager and checks if it has been already added</description>
        </method>
        <method name="RemoveDocument">
            <description>Removes the document from the transaction manager.</description>
        </method>
        <method name="Documents" qualifiers="const">
            <description>Returns the added documents to the transaction manager.</description>
        </method>
        <method name="SetNestedTransactionMode">
            <description>Sets nested transaction mode if isAllowed == Standard_True NOTE: field myIsNestedTransactionMode exists only for synchronization between several documents and has no effect on transactions of multitransaction manager.</description>
        </method>
        <method name="IsNestedTransactionMode" qualifiers="const">
            <description>Returns Standard_True if NestedTransaction mode is set. Methods for protection of changes outside transactions</description>
        </method>
        <method name="SetModificationMode">
            <description>If theTransactionOnly is True, denies all changes outside transactions.</description>
        </method>
        <method name="ModificationMode" qualifiers="const">
            <description>Returns True if changes are allowed only inside transactions.</description>
        </method>
        <method name="ClearUndos">
            <description>Clears undos in the manager and in documents.</description>
        </method>
        <method name="ClearRedos">
            <description>Clears redos in the manager and in documents.</description>
        </method>
    </methods>
</class>