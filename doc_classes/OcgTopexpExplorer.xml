<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTopexpExplorer" inherits="RefCounted">
    <brief_description>An Explorer is a Tool to visit a Topological Data Structure form the TopoDS package.</brief_description>
    <description>! An Explorer is a Tool to visit  a Topological Data
! Structure form the TopoDS package.
!
! An Explorer is built with :
!
! * The Shape to explore.
!
! * The type of Shapes to find : e.g VERTEX, EDGE.
! This type cannot be SHAPE.
!
! * The type of Shapes to avoid. e.g  SHELL, EDGE.
! By default   this type is  SHAPE which  means no
! restriction on the exploration.
!
! The Explorer  visits  all the  structure   to find
! shapes of the   requested  type  which   are   not
! contained in the type to avoid.
!
! Example to find all the Faces in the Shape S :
!
! TopExp_Explorer Ex;
! for (Ex.Init(S,TopAbs_FACE); Ex.More(); Ex.Next()) {
! ProcessFace(Ex.Current());
! }
!
! // an other way
! TopExp_Explorer Ex(S,TopAbs_FACE);
! while (Ex.More()) {
! ProcessFace(Ex.Current());
! Ex.Next();
! }
!
! To find all the vertices which are not in an edge :
!
! for (Ex.Init(S,TopAbs_VERTEX,TopAbs_EDGE); ...)
!
! To  find all the faces  in   a SHELL, then all the
! faces not in a SHELL :
!
! TopExp_Explorer Ex1, Ex2;
!
! for (Ex1.Init(S,TopAbs_SHELL),...) {
! // visit all shells
! for (Ex2.Init(Ex1.Current(),TopAbs_FACE),...) {
! // visit all the faces of the current shell
! }
! }
!
! for (Ex1.Init(S,TopAbs_FACE,TopAbs_SHELL),...) {
! // visit all faces not in a shell
! }
!
! If   the type  to avoid  is   the same  or is less
! complex than the type to find it has no effect.
!
! For example searching edges  not in a vertex  does
! not make a difference.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Creates an empty explorer, becomes useful after Init.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Creates an Explorer on the Shape &lt;S&gt;.</description>
        </method>
        <method name="Init">
            <description>Resets this explorer on the shape S. It is initialized to search the shape S, for shapes of type ToFind, that are not part of a shape ToAvoid. If the shape ToAvoid is equal to TopAbs_SHAPE, or if it is the same as, or less complex than, the shape ToFind it has no effect on the search.</description>
        </method>
        <method name="More" qualifiers="const">
            <description>Returns True if there are more shapes in the exploration.</description>
        </method>
        <method name="Next">
            <description>Moves to the next Shape in the exploration. Exceptions Standard_NoMoreObject if there are no more shapes to explore.</description>
        </method>
        <method name="Value" qualifiers="const">
            <description>Returns the current shape in the exploration. Exceptions Standard_NoSuchObject if this explorer has no more shapes to explore.</description>
        </method>
        <method name="Current" qualifiers="const">
            <description>Returns the current shape in the exploration. Exceptions Standard_NoSuchObject if this explorer has no more shapes to explore.</description>
        </method>
        <method name="ReInit">
            <description>Reinitialize the exploration with the original arguments.</description>
        </method>
        <method name="ExploredShape" qualifiers="const">
            <description>Return explored shape.</description>
        </method>
        <method name="Depth" qualifiers="const">
            <description>Returns the current depth of the exploration. 0 is the shape to explore itself.</description>
        </method>
        <method name="Clear">
            <description>Clears the content of the explorer. It will return False on More().</description>
        </method>
    </methods>
</class>