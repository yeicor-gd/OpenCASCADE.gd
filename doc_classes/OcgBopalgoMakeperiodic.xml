<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBopalgoMakeperiodic" inherits="RefCounted">
    <brief_description>BOPAlgo_MakePeriodic is the tool for making an arbitrary shape periodic in 3D space in specified directions.</brief_description>
    <description>! BOPAlgo_MakePeriodic is the tool for making an arbitrary shape periodic
! in 3D space in specified directions.
!
! Periodicity of the shape means that the shape can be repeated in any
! periodic direction any number of times without creation of the new
! geometry or splits.
!
! The idea is to make the shape look identical on the opposite sides of the
! periodic directions, so when translating the copy of a shape on the period
! there will be no coinciding parts of different dimensions.
!
! If necessary the algorithm will trim the shape to fit it into the
! requested period by splitting it by the planes limiting the shape's
! requested period.
!
! For making the shape periodic in certain direction the algorithm performs
! the following steps:
! * Creates the copy of the shape and moves it on the period into negative
!   side of the requested direction;
! * Splits the negative side of the shape by the moved copy, ensuring copying
!   of the geometry from positive side to negative;
! * Creates the copy of the shape (with already split negative side) and moves
!   it on the period into the positive side of the requested direction;
! * Splits the positive side of the shape by the moved copy, ensuring copying
!   of the geometry from negative side to positive.
!
! The algorithm also associates the identical (or twin) shapes located
! on the opposite sides of the result shape.
! Using the *GetTwins()* method it is possible to get the twin shapes from
! the opposite sides.
!
! Algorithm also provides the methods to repeat the periodic shape in
! periodic directions. The subsequent repetitions are performed on the
! repeated shape, thus repeating the shape two times in X direction will
! create result in three shapes (original plus two copies).
! Single subsequent repetition will result already in 6 shapes.
! The repetitions can be cleared and started over.
!
! The algorithm supports History of shapes modifications, thus
! it is possible to track how the shape has been changed to make it periodic
! and what new shapes have been created during repetitions.
!
! The algorithm supports the parallel processing mode, which allows faster
! completion of the operations.
!
! The algorithm supports the Error/Warning system and returns the following alerts:
! - *BOPAlgo_AlertNoPeriodicityRequired* - Error alert is given if no periodicity
!                                          has been requested in any direction;
! - *BOPAlgo_AlertUnableToTrim* - Error alert is given if the trimming of the shape
!                                 for fitting it into requested period has failed;
! - *BOPAlgo_AlertUnableToMakeIdentical* - Error alert is given if splitting of the
!                                          shape by its moved copies has failed;
! - *BOPAlgo_AlertUnableToRepeat* - Warning alert is given if the gluing of the repeated
!                                   shapes has failed.
!
! Example of usage of the algorithm:
! ~~~~
! TopoDS_Shape aShape = ...;                 // The shape to make periodic
! Standard_Boolean bMakeXPeriodic = ...;     // Flag for making or not the shape periodic in X
! direction Standard_Real aXPeriod = ...;              // X period for the shape Standard_Boolean
! isXTrimmed = ...;         // Flag defining whether it is necessary to trimming
!                                            // the shape to fit to X period
! Standard_Real aXFirst = ...;               // Start of the X period
!                                            // (really necessary only if the trimming is
!                                            requested)
! Standard_Boolean bRunParallel = ...;       // Parallel processing mode or single
!
! BOPAlgo_MakePeriodic aPeriodicityMaker;                   // Periodicity maker
! aPeriodicityMaker.SetShape(aShape);                       // Set the shape
! aPeriodicityMaker.MakeXPeriodic(bMakePeriodic, aXPeriod); // Making the shape periodic in X
! direction aPeriodicityMaker.SetTrimmed(isXTrimmed, aXFirst);        // Trim the shape to fit X
! period aPeriodicityMaker.SetRunParallel(bRunParallel);           // Set the parallel processing
! mode aPeriodicityMaker.Perform();                              // Performing the operation
!
! if (aPeriodicityMaker.HasErrors())                        // Check for the errors
! {
!   // errors treatment
!   Standard_SStream aSStream;
!   aPeriodicityMaker.DumpErrors(aSStream);
!   return;
! }
! if (aPeriodicityMaker.HasWarnings())                      // Check for the warnings
! {
!   // warnings treatment
!   Standard_SStream aSStream;
!   aPeriodicityMaker.DumpWarnings(aSStream);
! }
! const TopoDS_Shape&amp; aPeriodicShape = aPeriodicityMaker.Shape(); // Result periodic shape
!
!
! aPeriodicityMaker.XRepeat(2);                                    // Making repetitions
! const TopoDS_Shape&amp; aRepeat = aPeriodicityMaker.RepeatedShape(); // Getting the repeated shape
! aPeriodicityMaker.ClearRepetitions();                            // Clearing the repetitions
! ~~~~
!</description>
    <methods>
        <method name="BOPAlgo_MakePeriodic" qualifiers="static">
            <description>Empty constructor</description>
        </method>
        <method name="SetShape">
            <description>Sets the shape to make it periodic.</description>
        </method>
        <method name="MakePeriodic">
            <description>Sets the flag to make the shape periodic in specified direction: - 0 - X direction; - 1 - Y direction; - 2 - Z direction.</description>
        </method>
        <method name="IsPeriodic" qualifiers="const">
            <description>Returns the info about Periodicity of the shape in specified direction.</description>
        </method>
        <method name="Period" qualifiers="const">
            <description>Returns the Period of the shape in specified direction.</description>
        </method>
        <method name="MakeXPeriodic">
            <description>Sets the flag to make the shape periodic in X direction.</description>
        </method>
        <method name="IsXPeriodic" qualifiers="const">
            <description>Returns the info about periodicity of the shape in X direction.</description>
        </method>
        <method name="XPeriod" qualifiers="const">
            <description>Returns the XPeriod of the shape</description>
        </method>
        <method name="MakeYPeriodic">
            <description>Sets the flag to make the shape periodic in Y direction.</description>
        </method>
        <method name="IsYPeriodic" qualifiers="const">
            <description>Returns the info about periodicity of the shape in Y direction.</description>
        </method>
        <method name="YPeriod" qualifiers="const">
            <description>Returns the YPeriod of the shape.</description>
        </method>
        <method name="MakeZPeriodic">
            <description>Sets the flag to make the shape periodic in Z direction.</description>
        </method>
        <method name="IsZPeriodic" qualifiers="const">
            <description>Returns the info about periodicity of the shape in Z direction.</description>
        </method>
        <method name="ZPeriod" qualifiers="const">
            <description>Returns the ZPeriod of the shape.</description>
        </method>
        <method name="SetTrimmed">
            <description>Defines whether the input shape is already trimmed in specified direction to fit the period in this direction. Direction is defined by an ID: - 0 - X direction; - 1 - Y direction; - 2 - Z direction.</description>
        </method>
        <method name="IsInputTrimmed" qualifiers="const">
            <description>Returns whether the input shape was trimmed in the specified direction.</description>
        </method>
        <method name="PeriodFirst" qualifiers="const">
            <description>Returns the first periodic parameter in the specified direction.</description>
        </method>
        <method name="SetXTrimmed">
            <description>Defines whether the input shape is already trimmed in X direction to fit the X period. If the shape is not trimmed it is required to set the first parameter for the X period. The algorithm will make the shape fit into the period.</description>
        </method>
        <method name="IsInputXTrimmed" qualifiers="const">
            <description>Returns whether the input shape was already trimmed for X period.</description>
        </method>
        <method name="XPeriodFirst" qualifiers="const">
            <description>Returns the first parameter for the X period.</description>
        </method>
        <method name="SetYTrimmed">
            <description>Defines whether the input shape is already trimmed in Y direction to fit the Y period. If the shape is not trimmed it is required to set the first parameter for the Y period. The algorithm will make the shape fit into the period.</description>
        </method>
        <method name="IsInputYTrimmed" qualifiers="const">
            <description>Returns whether the input shape was already trimmed for Y period.</description>
        </method>
        <method name="YPeriodFirst" qualifiers="const">
            <description>Returns the first parameter for the Y period.</description>
        </method>
        <method name="SetZTrimmed">
            <description>Defines whether the input shape is already trimmed in Z direction to fit the Z period. If the shape is not trimmed it is required to set the first parameter for the Z period. The algorithm will make the shape fit into the period.</description>
        </method>
        <method name="IsInputZTrimmed" qualifiers="const">
            <description>Returns whether the input shape was already trimmed for Z period.</description>
        </method>
        <method name="ZPeriodFirst" qualifiers="const">
            <description>Returns the first parameter for the Z period.</description>
        </method>
        <method name="Perform">
            <description>Makes the shape periodic in necessary directions</description>
        </method>
        <method name="RepeatShape">
            <description>Performs repetition of the shape in specified direction required number of times. Negative value of times means that the repetition should be perform in negative direction. Makes the repeated shape a base for following repetitions.</description>
        </method>
        <method name="XRepeat">
            <description>Repeats the shape in X direction specified number of times. Negative value of times means that the repetition should be perform in negative X direction. Makes the repeated shape a base for following repetitions.</description>
        </method>
        <method name="YRepeat">
            <description>Repeats the shape in Y direction specified number of times. Negative value of times means that the repetition should be perform in negative Y direction. Makes the repeated shape a base for following repetitions.</description>
        </method>
        <method name="ZRepeat">
            <description>Repeats the shape in Z direction specified number of times. Negative value of times means that the repetition should be perform in negative Z direction. Makes the repeated shape a base for following repetitions.</description>
        </method>
        <method name="RepeatedShape" qualifiers="const">
            <description>Returns the repeated shape</description>
        </method>
        <method name="ClearRepetitions">
            <description>Clears all performed repetitions. The next repetition will be performed on the base shape.</description>
        </method>
        <method name="Shape" qualifiers="const">
            <description>Returns the resulting periodic shape</description>
        </method>
        <method name="GetTwins" qualifiers="const">
            <description>Returns the identical shapes for the given shape located on the opposite periodic side. Returns empty list in case the shape has no twin.</description>
        </method>
        <method name="History" qualifiers="const">
            <description>Returns the History of the algorithm</description>
        </method>
        <method name="Clear">
            <description>Clears the algorithm from previous runs</description>
        </method>
        <method name="ToDirectionID" qualifiers="static">
            <description>Converts the integer to ID of periodic direction</description>
        </method>
    </methods>
</class>