<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapeanalysisCurve" inherits="RefCounted">
    <brief_description>Analyzing tool for 2d or 3d curve. Computes parameters of projected point onto a curve.</brief_description>
    <description>! Analyzing tool for 2d or 3d curve.
! Computes parameters of projected point onto a curve.</description>
    <methods>
        <method name="Project_1" qualifiers="const">
            <description>Projects a Point on a Curve. Computes the projected point and its parameter on the curve. &lt;preci&gt; is used as 3d precision (hence, 0 will produce reject unless exact confusion). The number of iterations is limited. If AdjustToEnds is True, point will be adjusted to the end of the curve if distance is less than &lt;preci&gt;</description>
        </method>
        <method name="Project_2" qualifiers="const">
            <description>Projects a Point on a Curve. Computes the projected point and its parameter on the curve. &lt;preci&gt; is used as 3d precision (hence, 0 will produce reject unless exact confusion). The number of iterations is limited.</description>
        </method>
        <method name="Project_3" qualifiers="const">
            <description>Projects a Point on a Curve, but parameters are limited between &lt;cf&gt; and &lt;cl&gt;. The range [cf, cl] is extended with help of Adaptor3d on the basis of 3d precision &lt;preci&gt;. If AdjustToEnds is True, point will be adjusted to the end of the curve if distance is less than &lt;preci&gt;</description>
        </method>
        <method name="NextProject_2" qualifiers="const">
            <description>Projects a Point on a Curve using Newton method. &lt;paramPrev&gt; is taken as the first approximation of solution. If Newton algorithm fails the method Project() is used. If AdjustToEnds is True, point will be adjusted to the end of the curve if distance is less than &lt;preci&gt;</description>
        </method>
        <method name="NextProject_1" qualifiers="const">
            <description>Projects a Point on a Curve using Newton method. &lt;paramPrev&gt; is taken as the first approximation of solution. If Newton algorithm fails the method Project() is used.</description>
        </method>
        <method name="ValidateRange" qualifiers="const">
            <description>Validate parameters First and Last for the given curve in order to make them valid for creation of edge. This includes: - limiting range [First,Last] by range of curve - adjusting range [First,Last] for periodic (or closed) curve if Last &lt; First Returns True if parameters are OK or are successfully corrected, or False if parameters cannot be corrected. In the latter case, parameters are reset to range of curve.</description>
        </method>
        <method name="FillBndBox" qualifiers="const">
            <description>Computes a boundary box on segment of curve C2d from First to Last. This is done by taking NPoints points from the curve and, if Exact is True, by searching for exact extrema. All these points are added to Box.</description>
        </method>
        <method name="SelectForwardSeam" qualifiers="const">
            <description>Defines which pcurve (C1 or C2) should be chosen for FORWARD seam edge.</description>
        </method>
        <method name="IsPlanar_1" qualifiers="static">
            <description>Checks if points are planar with given preci. If Normal has not zero modulus, checks with given normal</description>
        </method>
        <method name="IsPlanar_2" qualifiers="static">
            <description>Checks if curve is planar with given preci. If Normal has not zero modulus, checks with given normal</description>
        </method>
        <method name="GetSamplePoints_1" qualifiers="static">
            <description>Returns sample points which will serve as linearisation of the2d curve in range (first, last) The distribution of sample points is consystent with what is used by BRepTopAdaptor_FClass2d</description>
        </method>
        <method name="GetSamplePoints_2" qualifiers="static">
            <description>Returns sample points which will serve as linearisation of the curve in range (first, last)</description>
        </method>
        <method name="IsClosed" qualifiers="static">
            <description>Tells if the Curve is closed with given precision. If &lt;preci&gt; &lt; 0 then Precision::Confusion is used.</description>
        </method>
        <method name="IsPeriodic_1" qualifiers="static">
            <description>This method was implemented as fix for changes in trimmed curve behaviour. For the moment trimmed curve returns false anyway. So it is necessary to adapt all Data exchange tools for this behaviour. Current implementation takes into account that curve may be offset.</description>
        </method>
        <method name="IsPeriodic_2" qualifiers="static">
            <description>The same as for Curve3d.</description>
        </method>
    </methods>
</class>