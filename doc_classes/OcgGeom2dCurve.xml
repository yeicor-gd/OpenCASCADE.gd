<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGeom2dCurve" inherits="RefCounted">
    <brief_description>The abstract class Curve describes the common behavior of curves in 2D space. The Geom2d package provides numerous concrete classes of derived curves, including lines, circles, conics, Bezier or BSpline curves, etc. The main characteristic of these curves is that they are parameterized. The Geom2d_Curve class shows: - how to work with the parametric equation of a curve in order to calculate the point of parameter u, together with the vector tangent and the derivative vectors of order 2, 3,..., N at this point; - how to obtain general information about the curve (for example, level of continuity, closed characteristics, periodicity, bounds of the parameter field); - how the parameter changes when a geometric transformation is applied to the curve or when the orientation of the curve is inverted. All curves must have a geometric continuity: a curve is at least "C0". Generally, this property is checked at the time of construction or when the curve is edited. Where this is not the case, the documentation explicitly states so. Warning The Geom2d package does not prevent the construction of curves with null length or curves which self-intersect.</brief_description>
    <description>! The abstract class Curve describes the common
! behavior of curves in 2D space. The Geom2d
! package provides numerous concrete classes of
! derived curves, including lines, circles, conics, Bezier
! or BSpline curves, etc.
! The main characteristic of these curves is that they
! are parameterized. The Geom2d_Curve class shows:
! - how to work with the parametric equation of a
! curve in order to calculate the point of parameter
! u, together with the vector tangent and the
! derivative vectors of order 2, 3,..., N at this point;
! - how to obtain general information about the curve
! (for example, level of continuity, closed
! characteristics, periodicity, bounds of the parameter field);
! - how the parameter changes when a geometric
! transformation is applied to the curve or when the
! orientation of the curve is inverted.
! All curves must have a geometric continuity: a curve is
! at least "C0". Generally, this property is checked at
! the time of construction or when the curve is edited.
! Where this is not the case, the documentation
! explicitly states so.
! Warning
! The Geom2d package does not prevent the
! construction of curves with null length or curves which
! self-intersect.</description>
    <methods>
        <method name="TransformedParameter" qualifiers="const">
            <description>Computes the parameter on the curve transformed by T for the point of parameter U on this curve. Note: this function generally returns U but it can be redefined (for example, on a line).</description>
        </method>
        <method name="ParametricTransformation" qualifiers="const">
            <description>Returns the coefficient required to compute the parametric transformation of this curve when transformation T is applied. This coefficient is the ratio between the parameter of a point on this curve and the parameter of the transformed point on the new curve transformed by T. Note: this function generally returns 1. but it can be redefined (for example, on a line).</description>
        </method>
        <method name="Reversed" qualifiers="const">
            <description>Creates a reversed duplicate Changes the orientation of this curve. The first and last parameters are not changed, but the parametric direction of the curve is reversed. If the curve is bounded: - the start point of the initial curve becomes the end point of the reversed curve, and - the end point of the initial curve becomes the start point of the reversed curve. - Reversed creates a new curve.</description>
        </method>
        <method name="Period" qualifiers="const">
            <description>Returns the period of this curve. raises if the curve is not periodic</description>
        </method>
        <method name="Value" qualifiers="const">
            <description>Computes the point of parameter U on &lt;me&gt;. If the curve is periodic then the returned point is P(U) with U = Ustart + (U - Uend) where Ustart and Uend are the parametric bounds of the curve.</description>
        </method>
        <method name="DumpJson" qualifiers="const">
            <description>Dumps the content of me into the stream</description>
        </method>
    </methods>
</class>