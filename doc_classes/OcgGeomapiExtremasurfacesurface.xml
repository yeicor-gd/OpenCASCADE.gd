<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGeomapiExtremasurfacesurface" inherits="RefCounted">
    <brief_description>Describes functions for computing all the extrema between two surfaces. An ExtremaSurfaceSurface algorithm minimizes or maximizes the distance between a point on the first surface and a point on the second surface. Results are start and end points of perpendiculars common to the two surfaces. Solutions consist of pairs of points, and an extremum is considered to be a segment joining the two points of a solution. An ExtremaSurfaceSurface object provides a framework for: - defining the construction of the extrema, - implementing the construction algorithm, and - consulting the results. Warning In some cases, the nearest points between the two surfaces do not correspond to one of the computed extrema. Instead, they may be given by: - a point of a bounding curve of one surface and one of the following: - its orthogonal projection on the other surface, - a point of a bounding curve of the other surface; or - any point on intersection curves between the two surfaces.</brief_description>
    <description>! Describes functions for computing all the extrema
! between two surfaces.
! An ExtremaSurfaceSurface algorithm minimizes or
! maximizes the distance between a point on the first
! surface and a point on the second surface. Results
! are start and end points of perpendiculars common to the two surfaces.
! Solutions consist of pairs of points, and an extremum
! is considered to be a segment joining the two points of a solution.
! An ExtremaSurfaceSurface object provides a framework for:
! -   defining the construction of the extrema,
! -   implementing the construction algorithm, and
! -   consulting the results.
! Warning
! In some cases, the nearest points between the two
! surfaces do not correspond to one of the computed
! extrema. Instead, they may be given by:
! -   a point of a bounding curve of one surface and one of the following:
! -   its orthogonal projection on the other surface,
! -   a point of a bounding curve of the other surface; or
! -   any point on intersection curves between the two surfaces.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Constructs an empty algorithm for computing extrema between two surfaces. Use an Init function to define the surfaces on which it is going to work.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Computes the extrema distances between the surfaces &lt;S1&gt; and &lt;S2&gt;</description>
        </method>
        <method name="from_3" qualifiers="static">
            <description>Computes the extrema distances between the portion of the surface S1 limited by the two values of parameter (U1min,U1max) in the u parametric direction, and by the two values of parameter (V1min,V1max) in the v parametric direction, and - the portion of the surface S2 limited by the two values of parameter (U2min,U2max) in the u parametric direction, and by the two values of parameter (V2min,V2max) in the v parametric direction.</description>
        </method>
        <method name="Init_1">
            <description>Initializes this algorithm with the given arguments and computes the extrema distances between the surfaces &lt;S1&gt; and &lt;S2&gt;</description>
        </method>
        <method name="Init_2">
            <description>Initializes this algorithm with the given arguments and computes the extrema distances between - the portion of the surface S1 limited by the two values of parameter (U1min,U1max) in the u parametric direction, and by the two values of parameter (V1min,V1max) in the v parametric direction, and - the portion of the surface S2 limited by the two values of parameter (U2min,U2max) in the u parametric direction, and by the two values of parameter (V2min,V2max) in the v parametric direction.</description>
        </method>
        <method name="NbExtrema" qualifiers="const">
            <description>Returns the number of extrema computed by this algorithm. Note: if this algorithm fails, NbExtrema returns 0.</description>
        </method>
        <method name="Points" qualifiers="const">
            <description>Returns the points P1 on the first surface and P2 on the second surface, which are the ends of the extremum of index Index computed by this algorithm. Exceptions Standard_OutOfRange if Index is not in the range [ 1,NbExtrema ], where NbExtrema is the number of extrema computed by this algorithm.</description>
        </method>
        <method name="Parameters" qualifiers="const">
            <description>Returns the parameters (U1,V1) of the point on the first surface, and (U2,V2) of the point on the second surface, which are the ends of the extremum of index Index computed by this algorithm. Exceptions Standard_OutOfRange if Index is not in the range [ 1,NbExtrema ], where NbExtrema is the number of extrema computed by this algorithm.</description>
        </method>
        <method name="Distance" qualifiers="const">
            <description>Computes the distance between the end points of the extremum of index Index computed by this algorithm. Exceptions Standard_OutOfRange if Index is not in the range [ 1,NbExtrema ], where NbExtrema is the number of extrema computed by this algorithm.</description>
        </method>
        <method name="IsParallel" qualifiers="const">
            <description>Returns True if the surfaces are parallel</description>
        </method>
        <method name="NearestPoints" qualifiers="const">
            <description>Returns the points P1 on the first surface and P2 on the second surface, which are the ends of the shortest extremum computed by this algorithm. Exceptions StdFail_NotDone if this algorithm fails.</description>
        </method>
        <method name="LowerDistanceParameters" qualifiers="const">
            <description>Returns the parameters (U1,V1) of the point on the first surface and (U2,V2) of the point on the second surface, which are the ends of the shortest extremum computed by this algorithm. Exceptions - StdFail_NotDone if this algorithm fails.</description>
        </method>
        <method name="LowerDistance" qualifiers="const">
            <description>Computes the distance between the end points of the shortest extremum computed by this algorithm. Exceptions StdFail_NotDone if this algorithm fails.</description>
        </method>
    </methods>
</class>