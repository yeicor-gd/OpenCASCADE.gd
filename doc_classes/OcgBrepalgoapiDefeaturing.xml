<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBrepalgoapiDefeaturing" inherits="RefCounted">
    <brief_description>The BRepAlgoAPI_Defeaturing algorithm is the API algorithm intended for removal of the unwanted parts from the shape. The unwanted parts (or features) can be holes, protrusions, gaps, chamfers, fillets etc. The shape itself is not modified, the new shape is built as the result.</brief_description>
    <description>! The BRepAlgoAPI_Defeaturing algorithm is the API algorithm intended for
! removal of the unwanted parts from the shape. The unwanted parts
! (or features) can be holes, protrusions, gaps, chamfers, fillets etc.
! The shape itself is not modified, the new shape is built as the result.
!
! The actual removal of the features from the shape is performed by
! the low-level *BOPAlgo_RemoveFeatures* tool. So the defeaturing algorithm
! has the same options, input data requirements, limitations as the
! low-level algorithm.
!
! &lt;b&gt;Input data&lt;/b&gt;
!
! Currently, only the shapes of type SOLID, COMPSOLID, and COMPOUND of Solids
! are supported. And only the FACEs can be removed from the shape.
!
! On the input the algorithm accepts the shape itself and the
! features which have to be removed. It does not matter how the features
! are given. It could be the separate faces or the collections
! of faces. The faces should belong to the initial shape, and those that
! do not belong will be ignored.
!
! &lt;b&gt;Options&lt;/b&gt;
!
! The algorithm has the following options:
! - History support;
!
! and the options available from base class:
! - Error/Warning reporting system;
! - Parallel processing mode.
!
! Please note that the other options of the base class are not supported
! here and will have no effect.
!
! For the details on the available options please refer to the description
! of *BOPAlgo_RemoveFeatures* algorithm.
!
! &lt;b&gt;Limitations&lt;/b&gt;
!
! The defeaturing algorithm has the same limitations as *BOPAlgo_RemoveFeatures*
! algorithm.
!
! &lt;b&gt;Example&lt;/b&gt;
!
! Here is the example of usage of the algorithm:
! ~~~~
! TopoDS_Shape aSolid = ...;               // Input shape to remove the features from
! TopTools_ListOfShape aFeatures = ...;    // Features to remove from the shape
! Standard_Boolean bRunParallel = ...;     // Parallel processing mode
! Standard_Boolean isHistoryNeeded = ...;  // History support
!
! BRepAlgoAPI_Defeaturing aDF;             // De-Featuring algorithm
! aDF.SetShape(aSolid);                    // Set the shape
! aDF.AddFacesToRemove(aFaces);            // Add faces to remove
! aDF.SetRunParallel(bRunParallel);        // Define the processing mode (parallel or single)
! aDF.SetToFillHistory(isHistoryNeeded);   // Define whether to track the shapes modifications
! aDF.Build();                             // Perform the operation
! if (!aDF.IsDone())                       // Check for the errors
! {
!   // error treatment
!   Standard_SStream aSStream;
!   aDF.DumpErrors(aSStream);
!   return;
! }
! if (aDF.HasWarnings())                   // Check for the warnings
! {
!   // warnings treatment
!   Standard_SStream aSStream;
!   aDF.DumpWarnings(aSStream);
! }
! const TopoDS_Shape&amp; aResult = aDF.Shape(); // Result shape
! ~~~~
!
! The algorithm preserves the type of the input shape in the result shape. Thus,
! if the input shape is a COMPSOLID, the resulting solids will also be put into a COMPSOLID.
!</description>
    <methods>
        <method name="BRepAlgoAPI_Defeaturing" qualifiers="static">
            <description>Empty constructor</description>
        </method>
        <method name="SetShape">
            <description>Sets the shape for processing.</description>
        </method>
        <method name="InputShape" qualifiers="const">
            <description>Returns the input shape</description>
        </method>
        <method name="AddFaceToRemove">
            <description>Adds the features to remove from the input shape.</description>
        </method>
        <method name="AddFacesToRemove">
            <description>Adds the faces to remove from the input shape.</description>
        </method>
        <method name="FacesToRemove" qualifiers="const">
            <description>Returns the list of faces which have been requested for removal from the input shape.</description>
        </method>
        <method name="Build">
            <description>Performs the operation</description>
        </method>
        <method name="SetToFillHistory">
            <description>Defines whether to track the modification of the shapes or not.</description>
        </method>
        <method name="HasHistory" qualifiers="const">
            <description>Returns whether the history was requested or not.</description>
        </method>
        <method name="Modified">
            <description>Returns the list of shapes modified from the shape &lt;theS&gt; during the operation.</description>
        </method>
        <method name="Generated">
            <description>Returns the list of shapes generated from the shape &lt;theS&gt; during the operation.</description>
        </method>
        <method name="IsDeleted">
            <description>Returns true if the shape &lt;theS&gt; has been deleted during the operation. It means that the shape has no any trace in the result. Otherwise it returns false.</description>
        </method>
        <method name="HasModified" qualifiers="const">
            <description>Returns true if any of the input shapes has been modified during operation.</description>
        </method>
        <method name="HasGenerated" qualifiers="const">
            <description>Returns true if any of the input shapes has generated shapes during operation.</description>
        </method>
        <method name="HasDeleted" qualifiers="const">
            <description>Returns true if any of the input shapes has been deleted during operation.</description>
        </method>
        <method name="History">
            <description>Returns the History of shapes modifications</description>
        </method>
    </methods>
</class>