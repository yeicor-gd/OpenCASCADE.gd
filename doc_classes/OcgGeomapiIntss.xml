<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGeomapiIntss" inherits="RefCounted">
    <brief_description>This class implements methods for computing the intersection curves between two surfaces. The result is curves from Geom. The "domain" used for a surface is the natural parametric domain unless the surface is a RectangularTrimmedSurface from Geom.</brief_description>
    <description>! This class implements methods for
! computing the intersection curves   between two surfaces.
! The result is curves from Geom.  The "domain" used for
! a surface   is the natural  parametric domain
! unless the surface is a  RectangularTrimmedSurface
! from Geom.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Constructs an empty object. Use the function Perform for further initialization algorithm by two surfaces.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Computes the intersection curves between the two surfaces S1 and S2. Parameter Tol defines the precision of curves computation. For most cases the value 1.0e-7 is recommended to use. Warning Use the function IsDone to verify that the intersections are successfully computed.I</description>
        </method>
        <method name="Perform">
            <description>Initializes an algorithm with the given arguments and computes the intersection curves between the two surfaces S1 and S2. Parameter Tol defines the precision of curves computation. For most cases the value 1.0e-7 is recommended to use. Warning Use function IsDone to verify that the intersections are successfully computed.</description>
        </method>
        <method name="IsDone" qualifiers="const">
            <description>Returns True if the intersection was successful.</description>
        </method>
        <method name="NbLines" qualifiers="const">
            <description>Returns the number of computed intersection curves. Exceptions StdFail_NotDone if the computation fails.</description>
        </method>
        <method name="Line" qualifiers="const">
            <description>Returns the computed intersection curve of index Index. Exceptions StdFail_NotDone if the computation fails. Standard_OutOfRange if Index is out of range [1, NbLines] where NbLines is the number of computed intersection curves.</description>
        </method>
    </methods>
</class>