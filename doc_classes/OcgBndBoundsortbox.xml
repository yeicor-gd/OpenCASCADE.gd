<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBndBoundsortbox" inherits="RefCounted">
    <brief_description>A tool to compare a bounding box or a plane with a set of bounding boxes. It sorts the set of bounding boxes to give the list of boxes which intersect the element being compared. The boxes being sorted generally bound a set of shapes, while the box being compared bounds a shape to be compared. The resulting list of intersecting boxes therefore gives the list of items which potentially intersect the shape to be compared. How to use this class: - Create an instance of this class. - Initialize it with the set of boxes to be sorted using one of the Initialize() methods. - Call the Compare() method with the box or plane to be compared. Compare() will return the list of indices of the boxes which intersect the box or plane passed as argument.</brief_description>
    <description>! A tool to compare a bounding box or a plane with a set of
! bounding boxes. It sorts the set of bounding boxes to give
! the list of boxes which intersect the element being compared.
! The boxes being sorted generally bound a set of shapes,
! while the box being compared bounds a shape to be
! compared. The resulting list of intersecting boxes therefore
! gives the list of items which potentially intersect the shape to be compared.
! How to use this class:
! - Create an instance of this class.
! - Initialize it with the set of boxes to be sorted using one of the
!   Initialize() methods.
! - Call the Compare() method with the box or plane to be compared.
!   Compare() will return the list of indices of the boxes which intersect
!   the box or plane passed as argument.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Constructs an empty comparison algorithm for bounding boxes. The bounding boxes are then defined using the Initialize function.</description>
        </method>
        <method name="Initialize">
            <description>Initializes this comparison algorithm with the bounding box that encloses all the boxes that will be used by this algorithm. and the expected number of those boxes. Boxes to be considered can then be added using the Add() method.</description>
        </method>
        <method name="Add">
            <description>Adds the bounding box theBox at position boxIndex in the internal array of boxes to be sorted by this comparison algorithm. This function is used only in conjunction with the Initialize(const Bnd_Box&amp;, const int) method. Exceptions: - Standard_OutOfRange if boxIndex is not in the range [ 1,nbComponents ] where nbComponents is the maximum number of bounding boxes declared for this algorithm at initialization. - Standard_MultiplyDefined if a box already exists at position theIndex in the internal array of boxes.</description>
        </method>
        <method name="Compare_1">
            <description>Compares the bounding box theBox, with the set of bounding boxes provided to this algorithm at initialization, and returns the list of indices of bounding boxes that intersect the theBox or are inside it. The indices correspond to the indices of the bounding boxes in the array provided to this algorithm at initialization.</description>
        </method>
        <method name="Compare_2">
            <description>Compares the plane thePlane with the set of bounding boxes provided to this algorithm at initialization, and returns the list of indices of bounding boxes that intersect the thePlane. The indices correspond to the indices of the bounding boxes in the array provided to this algorithm at initialization.</description>
        </method>
    </methods>
</class>