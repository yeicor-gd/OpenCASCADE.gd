<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapebuildEdge" inherits="RefCounted">
    <brief_description>This class provides low-level operators for building an edge 3d curve, copying edge with replaced vertices etc.</brief_description>
    <description>! This class provides low-level operators for building an edge
! 3d curve, copying edge with replaced vertices etc.</description>
    <methods>
        <method name="CopyReplaceVertices" qualifiers="const">
            <description>Copy edge and replace one or both its vertices to a given one(s). Vertex V1 replaces FORWARD vertex, and V2 - REVERSED, as they are found by TopoDS_Iterator. If V1 or V2 is NULL, the original vertex is taken</description>
        </method>
        <method name="CopyRanges" qualifiers="const">
            <description>Copies ranges for curve3d and all common pcurves from edge &lt;fromedge&gt; into edge &lt;toedge&gt;.</description>
        </method>
        <method name="SetRange3d" qualifiers="const">
            <description>Sets range on 3d curve only.</description>
        </method>
        <method name="CopyPCurves" qualifiers="const">
            <description>Makes a copy of pcurves from edge &lt;fromedge&gt; into edge &lt;toedge&gt;. Pcurves which are already present in &lt;toedge&gt;, are replaced by copies, other are copied. Ranges are also copied.</description>
        </method>
        <method name="Copy" qualifiers="const">
            <description>Make a copy of &lt;edge&gt; by call to CopyReplaceVertices() (i.e. construct new TEdge with the same pcurves and vertices). If &lt;sharepcurves&gt; is False, pcurves are also replaced by their copies with help of method CopyPCurves</description>
        </method>
        <method name="RemovePCurve" qualifiers="const">
            <description>Removes the PCurve(s) which could be recorded in an Edge for the given Face</description>
        </method>
        <method name="ReassignPCurve" qualifiers="const">
            <description>Reassign edge pcurve lying on face &lt;old&gt; to another face . If edge has two pcurves on &lt;old&gt; face, only one of them will be reassigned, and other will left alone. Similarly, if edge already had a pcurve on face , it will have two pcurves on it. Returns True if succeeded, False if no pcurve lying on &lt;old&gt; found.</description>
        </method>
        <method name="RemoveCurve3d" qualifiers="const">
            <description>Removes the Curve3D recorded in an Edge</description>
        </method>
        <method name="BuildCurve3d" qualifiers="const">
            <description>Calls BRepTools::BuildCurve3D</description>
        </method>
    </methods>
</class>