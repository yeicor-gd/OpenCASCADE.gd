<?xml version='1.0' encoding='utf-8'?>
<class name="OcgXscontrolConnectedshapes" inherits="RefCounted">
    <brief_description>From a TopoDS_Shape, or from the entity which has produced it, searches for the shapes, and the entities which have produced them in last transfer, which are adjacent to it by VERTICES</brief_description>
    <description>! From a TopoDS_Shape, or from the entity which has produced it,
! searches for the shapes, and the entities which have produced
! them in last transfer, which are adjacent to it by VERTICES</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Creates a Selection ConnectedShapes. It remains to be set a TransferReader</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Creates a Selection ConnectedShapes, which will work with the current TransferProcess brought by the TransferReader</description>
        </method>
        <method name="SetReader">
            <description>Sets a TransferReader to sort entities : it brings the TransferProcess which may change, while the TransferReader does not</description>
        </method>
        <method name="AdjacentEntities" qualifiers="static">
            <description>This functions considers a shape from a transfer and performs the search function explained above</description>
        </method>
    </methods>
</class>