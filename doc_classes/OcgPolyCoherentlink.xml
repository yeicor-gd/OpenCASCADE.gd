<?xml version='1.0' encoding='utf-8'?>
<class name="OcgPolyCoherentlink" inherits="RefCounted">
    <brief_description>Link between two mesh nodes that is created by existing triangle(s). Keeps reference to the opposite node of each incident triangle. The referred node with index "0" is always on the left side of the link, the one with the index "1" is always on the right side. It is possible to find both incident triangles using the method Poly_CoherentTriangulation::FindTriangle(). Any Link can store an arbitrary pointer that is called Attribute.</brief_description>
    <description>/**
 * Link between two mesh nodes that is created by existing triangle(s).
 * Keeps reference to the opposite node of each incident triangle.
 * The referred node with index "0" is always on the left side of the link,
 * the one with the index "1" is always on the right side.
 * It is possible to find both incident triangles using the method
 * Poly_CoherentTriangulation::FindTriangle().
 * &lt;p&gt;
 * Any Link can store an arbitrary pointer that is called Attribute.
 */</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Empty constructor.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Constructor. Creates a Link that has no reference to 'opposite nodes'. This constructor is useful to create temporary object that is not inserted into any existing triangulation.</description>
        </method>
        <method name="from_3" qualifiers="static">
            <description>Constructor, takes a triangle and a side. A link is created always such that myNode[0] &lt; myNode[1]. Unlike the previous constructor, this one assigns the 'opposite node' fields. This constructor is used when a link is inserted into a Poly_CoherentTriangulation structure.</description>
        </method>
        <method name="Node" qualifiers="const">
            <description>Return the node index in the current triangulation.</description>
        </method>
        <method name="OppositeNode" qualifiers="const">
            <description>Return the opposite node (belonging to the left or right incident triangle) index in the current triangulation.</description>
        </method>
        <method name="Nullify">
            <description>Invalidate this Link.</description>
        </method>
    </methods>
</class>