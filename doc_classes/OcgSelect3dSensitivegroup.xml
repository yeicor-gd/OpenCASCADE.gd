<?xml version='1.0' encoding='utf-8'?>
<class name="OcgSelect3dSensitivegroup" inherits="RefCounted">
    <brief_description>A framework to define selection of a sensitive group by a sensitive entity which is a set of 3D sensitive entities. Remark: 2 modes are possible for rectangle selection the group is considered selected 1) when all the entities inside are selected in the rectangle 2) only one entity inside is selected by the rectangle By default the "Match All entities" mode is set.</brief_description>
    <description>! A framework to define selection of a sensitive group
!          by a sensitive entity which is a set of 3D sensitive entities.
!          Remark: 2 modes are possible for rectangle selection
!          the group is considered selected
!          1) when all the entities inside are selected in the rectangle
!          2) only one entity inside is selected by the rectangle
!          By default the "Match All entities" mode is set.</description>
    <methods>
        <method name="Clear">
            <description>Removes all sensitive entities from the list used at the time of construction, or added using the function Add.</description>
        </method>
        <method name="SetMatchType">
            <description>Sets the requirement that all sensitive entities in the list used at the time of construction, or added using the function Add must be matched.</description>
        </method>
        <method name="NbSubElements" qualifiers="const">
            <description>Returns the amount of sub-entities</description>
        </method>
        <method name="CenterOfGeometry" qualifiers="const">
            <description>Returns center of entity set. If location transformation is set, it will be applied</description>
        </method>
        <method name="Center" qualifiers="const">
            <description>Returns geometry center of sensitive entity index theIdx in the vector along the given axis theAxis</description>
        </method>
        <method name="Swap">
            <description>Swaps items with indexes theIdx1 and theIdx2 in the vector</description>
        </method>
        <method name="Size" qualifiers="const">
            <description>Returns the length of vector of sensitive entities</description>
        </method>
        <method name="DumpJson" qualifiers="const">
            <description>Dumps the content of me into the stream</description>
        </method>
    </methods>
</class>