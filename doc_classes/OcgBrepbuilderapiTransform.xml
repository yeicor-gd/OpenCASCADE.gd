<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBrepbuilderapiTransform" inherits="RefCounted">
    <brief_description>Geometric transformation on a shape. The transformation to be applied is defined as a gp_Trsf transformation, i.e. a transformation which does not modify the underlying geometry of shapes. The transformation is applied to: - all curves which support edges of a shape, and - all surfaces which support its faces. A Transform object provides a framework for: - defining the geometric transformation to be applied, - implementing the transformation algorithm, and - consulting the results.</brief_description>
    <description>! Geometric transformation on a shape.
! The transformation to be applied is defined as a
! gp_Trsf transformation, i.e. a transformation which does
! not modify the underlying geometry of shapes.
! The transformation is applied to:
! -   all curves which support edges of a shape, and
! -   all surfaces which support its faces.
! A Transform object provides a framework for:
! -   defining the geometric transformation to be applied,
! -   implementing the transformation algorithm, and
! -   consulting the results.</description>
    <methods>
        <method name="from_gp_Trsf_const_ref" qualifiers="static">
            <description>Constructs a framework for applying the geometric transformation T to a shape. Use the function Perform to define the shape to transform.</description>
        </method>
        <method name="from_TopoDS_Shape_const_ref_gp_Trsf_const_ref_bool_bool" qualifiers="static">
            <description>Creates a transformation from the gp_Trsf &lt;theTrsf&gt;, and applies it to the shape &lt;theShape&gt;. If the transformation is direct and isometric (determinant = 1) and &lt;theCopyGeom&gt; = false, the resulting shape is &lt;theShape&gt; on which a new location has been set. Otherwise, the transformation is applied on a duplication of &lt;theShape&gt;. If &lt;theCopyMesh&gt; is true, the triangulation will be copied, and the copy will be assigned to the result shape.</description>
        </method>
        <method name="Perform">
            <description>Applies the geometric transformation defined at the time of construction of this framework to the shape S. - If the transformation T is direct and isometric, in other words, if the determinant of the vectorial part of T is equal to 1., and if theCopyGeom equals false (the default value), the resulting shape is the same as the original but with a new location assigned to it. - In all other cases, the transformation is applied to a duplicate of theShape. - If theCopyMesh is true, the triangulation will be copied, and the copy will be assigned to the result shape. Use the function Shape to access the result. Note: this framework can be reused to apply the same geometric transformation to other shapes. You only need to specify them by calling the function Perform again.</description>
        </method>
        <method name="ModifiedShape" qualifiers="const">
            <description>Returns the modified shape corresponding to &lt;S&gt;.</description>
        </method>
    </methods>
</class>