<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGeomapiIntcs" inherits="RefCounted">
    <brief_description>This class implements methods for computing intersection points and segments between a</brief_description>
    <description>! This class implements methods for
! computing intersection points and  segments between a</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Creates an empty object. Use the function Perform for further initialization of the algorithm by the curve and the surface.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Computes the intersections between the curve C and the surface S. Warning Use function IsDone to verify that the intersections are computed successfully.</description>
        </method>
        <method name="Perform">
            <description>This function Initializes an algorithm with the curve C and the surface S and computes the intersections between C and S. Warning Use function IsDone to verify that the intersections are computed successfully.</description>
        </method>
        <method name="IsDone" qualifiers="const">
            <description>Returns true if the intersections are successfully computed.</description>
        </method>
        <method name="NbPoints" qualifiers="const">
            <description>Returns the number of Intersection Points if IsDone returns True. else NotDone is raised.</description>
        </method>
        <method name="Point" qualifiers="const">
            <description>Returns the Intersection Point of range &lt;Index&gt;in case of cross intersection. Raises NotDone if the computation has failed or if the computation has not been done raises OutOfRange if Index is not in the range &lt;1..NbPoints&gt;</description>
        </method>
        <method name="Parameters_1" qualifiers="const">
            <description>Returns parameter W on the curve and (parameters U,V) on the surface of the computed intersection point of index Index in case of cross intersection. Exceptions StdFail_NotDone if intersection algorithm fails or is not initialized. Standard_OutOfRange if Index is not in the range [ 1,NbPoints ], where NbPoints is the number of computed intersection points.</description>
        </method>
        <method name="NbSegments" qualifiers="const">
            <description>Returns the number of computed intersection segments in case of tangential intersection. Exceptions StdFail_NotDone if the intersection algorithm fails or is not initialized.</description>
        </method>
        <method name="Segment" qualifiers="const">
            <description>Returns the computed intersection segment of index Index in case of tangential intersection. Intersection segment is a portion of the initial curve tangent to surface. Exceptions StdFail_NotDone if intersection algorithm fails or is not initialized. Standard_OutOfRange if Index is not in the range [ 1,NbSegments ], where NbSegments is the number of computed intersection segments.</description>
        </method>
        <method name="Parameters_2" qualifiers="const">
            <description>Returns the parameters of the first (U1,V1) and the last (U2,V2) points of curve's segment on the surface in case of tangential intersection. Index is the number of computed intersection segments. Exceptions StdFail_NotDone if intersection algorithm fails or is not initialized. Standard_OutOfRange if Index is not in the range [ 1,NbSegments ], where NbSegments is the number of computed intersection segments.</description>
        </method>
    </methods>
</class>