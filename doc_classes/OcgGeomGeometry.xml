<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGeomGeometry" inherits="RefCounted">
    <brief_description>The abstract class Geometry for 3D space is the root class of all geometric objects from the Geom package. It describes the common behavior of these objects when: - applying geometric transformations to objects, and - constructing objects by geometric transformation (including copying). Warning Only transformations which do not modify the nature of the geometry can be applied to Geom objects: this is the case with translations, rotations, symmetries and scales; this is also the case with gp_Trsf composite transformations which are used to define the geometric transformations applied using the Transform or Transformed functions. Note: Geometry defines the "prototype" of the abstract method Transform which is defined for each concrete type of derived object. All other transformations are implemented using the Transform method.</brief_description>
    <description>! The abstract class Geometry for 3D space is the root
! class of all geometric objects from the Geom
! package. It describes the common behavior of these objects when:
! - applying geometric transformations to objects, and
! - constructing objects by geometric transformation (including copying).
! Warning
! Only transformations which do not modify the nature
! of the geometry can be applied to Geom objects: this
! is the case with translations, rotations, symmetries
! and scales; this is also the case with gp_Trsf
! composite transformations which are used to define
! the geometric transformations applied using the
! Transform or Transformed functions.
! Note: Geometry defines the "prototype" of the
! abstract method Transform which is defined for each
! concrete type of derived object. All other
! transformations are implemented using the Transform method.</description>
    <methods>
        <method name="Mirror_1">
            <description>Performs the symmetrical transformation of a Geometry with respect to the point P which is the center of the symmetry.</description>
        </method>
        <method name="Mirror_2">
            <description>Performs the symmetrical transformation of a Geometry with respect to an axis placement which is the axis of the symmetry.</description>
        </method>
        <method name="Mirror_3">
            <description>Performs the symmetrical transformation of a Geometry with respect to a plane. The axis placement A2 locates the plane of the symmetry : (Location, XDirection, YDirection).</description>
        </method>
        <method name="Rotate">
            <description>Rotates a Geometry. A1 is the axis of the rotation. Ang is the angular value of the rotation in radians.</description>
        </method>
        <method name="Scale">
            <description>Scales a Geometry. S is the scaling value.</description>
        </method>
        <method name="Translate_1">
            <description>Translates a Geometry. V is the vector of the translation.</description>
        </method>
        <method name="Translate_2">
            <description>Translates a Geometry from the point P1 to the point P2.</description>
        </method>
        <method name="DumpJson" qualifiers="const">
            <description>Dumps the content of me into the stream</description>
        </method>
    </methods>
</class>