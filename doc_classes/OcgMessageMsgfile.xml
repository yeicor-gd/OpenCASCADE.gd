<?xml version='1.0' encoding='utf-8'?>
<class name="OcgMessageMsgfile" inherits="RefCounted">
    <brief_description>A tool providing facility to load definitions of message strings from resource file(s).</brief_description>
    <description>! A tool providing facility to load definitions of message strings from
! resource file(s).
!
! The message file is an ASCII file which defines a set of messages.
! Each message is identified by its keyword (string).
!
! All lines in the file starting with the exclamation sign
! (perhaps preceding by spaces and/or tabs) are ignored as comments.
!
! Each line in the file starting with the dot character "."
! (perhaps preceding by spaces and/or tabs) defines the keyword.
! The keyword is a string starting from the next symbol after dot
! and ending at the symbol preceding ending newline character "\n".
!
! All the lines in the file after the keyword and before next
! keyword (and which are not comments) define the message for that
! keyword. If the message consists of several lines, the message
! string will contain newline symbols "\n" between parts (but not
! at the end).
!
! The experimental support of Unicode message files is provided.
! These are distinguished by two bytes FF.FE or FE.FF at the beginning.
!
! The loaded messages are stored in static data map; all methods of that
! class are also static.</description>
    <methods>
        <method name="Load" qualifiers="static">
            <description>Load message file &lt;theFileName&gt; from directory &lt;theDirName&gt; or its sub-directory</description>
        </method>
        <method name="LoadFile" qualifiers="static">
            <description>Load the messages from the given file, additive to any previously loaded messages. Messages with same keywords, if already present, are replaced with the new ones.</description>
        </method>
        <method name="LoadFromEnv" qualifiers="static">
            <description>Loads the messages from the file with name (without extension) given by environment variable. Extension of the file name is given separately. If its not defined, it is taken: - by default from environment CSF_LANGUAGE, - if not defined either, as "us".</description>
        </method>
        <method name="LoadFromString" qualifiers="static">
            <description>Loads the messages from the given text buffer.</description>
        </method>
        <method name="AddMsg" qualifiers="static">
            <description>Adds new message to the map. Parameter &lt;key&gt; gives the key of the message, &lt;text&gt; defines the message itself. If there already was defined the message identified by the same keyword, it is replaced with the new one.</description>
        </method>
        <method name="HasMsg" qualifiers="static">
            <description>Returns True if message with specified keyword is registered</description>
        </method>
        <method name="Msg_2" qualifiers="static">
            <description>Gives the text for the message identified by the keyword &lt;key&gt;. If there are no messages with such keyword defined, the error message is returned. In that case reference to static string is returned, it can be changed with next call(s) to Msg(). Note: The error message is constructed like 'Unknown message: &lt;key&gt;', and can itself be customized by defining message with key Message_Msg_BadKeyword.</description>
        </method>
    </methods>
</class>