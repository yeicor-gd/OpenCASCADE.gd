<?xml version='1.0' encoding='utf-8'?>
<class name="OcgPrsdimRadiusdimension" inherits="RefCounted">
    <brief_description>Radius dimension. Can be constructed: - On generic circle. - On generic circle with user-defined anchor point on that circle. - On generic shape containing geometry that can be measured by diameter dimension: circle wire, arc, circular face, etc. The anchor point is the location of left attachment point of dimension on the circle. It can be user-specified, or computed as middle point on the arc. The radius dimension always lies in the plane of the measured circle. The dimension is considered as invalid if the user-specified anchor point is not lying on the circle, if the radius of the circle is less than Precision::Confusion(). In case if the dimension is built on the arbitrary shape, it can be considered as invalid if the shape does not contain circle geometry.</brief_description>
    <description>! Radius dimension. Can be constructed:
! - On generic circle.
! - On generic circle with user-defined anchor point on that circle.
! - On generic shape containing geometry that can be measured
!   by diameter dimension: circle wire, arc, circular face, etc.
! The anchor point is the location of left attachment point of
! dimension on the circle. It can be user-specified, or computed as
! middle point on the arc. The radius dimension always lies in the
! plane of the measured circle. The dimension is considered as
! invalid if the user-specified anchor point is not lying on the circle,
! if the radius of the circle is less than Precision::Confusion().
! In case if the dimension is built on the arbitrary shape,
! it can be considered as invalid if the shape does not contain
! circle geometry.</description>
    <methods>
        <method name="from_gp_Circ_const_ref" qualifiers="static">
            <description>Create radius dimension for the circle geometry.</description>
        </method>
        <method name="from_TopoDS_Shape_const_ref" qualifiers="static">
            <description>Create radius dimension for the arbitrary shape (if possible).</description>
        </method>
        <method name="from_gp_Circ_const_ref_gp_Pnt_const_ref" qualifiers="static">
            <description>Create radius dimension for the circle geometry and define its orientation by location of the first point on that circle.</description>
        </method>
        <method name="Circle" qualifiers="const">
            <description>Returns measured geometry circle.</description>
        </method>
        <method name="AnchorPoint" qualifiers="const">
            <description>Returns anchor point on circle for radius dimension.</description>
        </method>
        <method name="Shape" qualifiers="const">
            <description>Returns the measured shape.</description>
        </method>
        <method name="SetMeasuredGeometry_1">
            <description>Measure radius of the circle. The dimension will become invalid if the radius of the circle is less than Precision::Confusion().</description>
        </method>
        <method name="SetMeasuredGeometry_3">
            <description>Measure radius of the circle and orient the dimension so the dimension lines attaches to anchor point on the circle. The dimension will become invalid if the radius of the circle is less than Precision::Confusion().</description>
        </method>
        <method name="SetMeasuredGeometry_2">
            <description>Measure radius on the passed shape, if applicable. The dimension will become invalid if the passed shape is not measurable or if measured diameter value is less than Precision::Confusion().</description>
        </method>
        <method name="SetMeasuredGeometry_4">
            <description>Measure radius on the passed shape, if applicable. The dimension will become invalid if the passed shape is not measurable or if measured diameter value is less than Precision::Confusion().</description>
        </method>
        <method name="GetDisplayUnits" qualifiers="const">
            <description>Returns the display units string.</description>
        </method>
        <method name="GetModelUnits" qualifiers="const">
            <description>Returns the model units string.</description>
        </method>
    </methods>
</class>