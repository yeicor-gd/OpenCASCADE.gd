<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapefixWire" inherits="RefCounted">
    <brief_description>This class provides a set of tools for repairing a wire.</brief_description>
    <description>! This class provides a set of tools for repairing a wire.
!
! These are methods Fix...(), organised in two levels:
!
! Level 1: Advanced - each method in this level fixes one separate problem,
! usually dealing with either single edge or connection of the
! two adjacent edges. These methods should be used carefully and
! called in right sequence, because some of them depend on others.
!
! Level 2: Public (API) - methods which group several methods of level 1
! and call them in a proper sequence in order to make some
! consistent set of fixes for a whole wire. It is possible to
! control calls to methods of the advanced level from methods of
! the public level by use of flags Fix..Mode() (see below).
!
! Fixes can be made in three ways:
! 1. Increasing tolerance of an edge or a vertex
! 2. Changing topology (adding/removing/replacing edge in the wire
! and/or replacing the vertex in the edge)
! 3. Changing geometry (shifting vertex or adjusting ends of edge
! curve to vertices, or recomputing curves of the edge)
!
! When fix can be made in more than one way (e.g., either
! by increasing tolerance or shifting a vertex), it is chosen
! according to the flags:
! ModifyTopologyMode - allows modification of the topology.
! This flag can be set when fixing a wire on
! the separate (free) face, and should be
! unset for face which is part of shell.
! ModifyGeometryMode - allows modification of the geometry.
!
! The order of descriptions of Fix() methods in this CDL
! approximately corresponds to the optimal order of calls.
!
! NOTE: most of fixing methods expect edges in the
! ShapeExtend_WireData to be ordered, so it is necessary to make
! call to FixReorder() before any other fixes
!
! ShapeFix_Wire should be initialized prior to any fix by the
! following data:
! a) Wire (ether TopoDS_Wire or ShapeExtend_Wire)
! b) Face or surface
! c) Precision
! d) Maximal tail angle and width
! This can be done either by calling corresponding methods
! (LoadWire, SetFace or SetSurface, SetPrecision, SetMaxTailAngle
! and SetMaxTailWidth), or
! by loading already filled ShapeAnalisis_Wire with method Load</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Empty Constructor, creates clear object with default flags</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Create new object with default flags and prepare it for use (Loads analyzer with all the data for the wire and face)</description>
        </method>
        <method name="ClearModes">
            <description>Sets all modes to default</description>
        </method>
        <method name="ClearStatuses">
            <description>Clears all statuses</description>
        </method>
        <method name="Init_2">
            <description>Load analyzer with all the data for the wire and face and drops all fixing statuses</description>
        </method>
        <method name="Init_1">
            <description>Load analyzer with all the data already prepared and drops all fixing statuses If analyzer contains face, there is no need to set it by SetFace or SetSurface</description>
        </method>
        <method name="Load_1">
            <description>Load data for the wire, and drops all fixing statuses</description>
        </method>
        <method name="Load_2">
            <description>Load data for the wire, and drops all fixing statuses</description>
        </method>
        <method name="SetFace">
            <description>Set working face for the wire</description>
        </method>
        <method name="SetSurface_1">
            <description>Set surface for the wire</description>
        </method>
        <method name="SetSurface_2">
            <description>Set surface for the wire</description>
        </method>
        <method name="SetPrecision">
            <description>Set working precision (to root and to analyzer)</description>
        </method>
        <method name="SetMaxTailAngle">
            <description>Sets the maximal allowed angle of the tails in radians.</description>
        </method>
        <method name="SetMaxTailWidth">
            <description>Sets the maximal allowed width of the tails.</description>
        </method>
        <method name="IsLoaded" qualifiers="const">
            <description>Tells if the wire is loaded</description>
        </method>
        <method name="IsReady" qualifiers="const">
            <description>Tells if the wire and face are loaded</description>
        </method>
        <method name="NbEdges" qualifiers="const">
            <description>returns number of edges in the working wire</description>
        </method>
        <method name="Wire" qualifiers="const">
            <description>Makes the resulting Wire (by basic Brep_Builder)</description>
        </method>
        <method name="WireAPIMake" qualifiers="const">
            <description>Makes the resulting Wire (by BRepAPI_MakeWire)</description>
        </method>
        <method name="Analyzer" qualifiers="const">
            <description>returns field Analyzer (working tool)</description>
        </method>
        <method name="WireData" qualifiers="const">
            <description>returns working wire</description>
        </method>
        <method name="Face" qualifiers="const">
            <description>returns working face (Analyzer.Face())</description>
        </method>
        <method name="ModifyTopologyMode">
            <description>Returns (modifiable) the flag which defines whether it is allowed to modify topology of the wire during fixing (adding/removing edges etc.)</description>
        </method>
        <method name="ModifyGeometryMode">
            <description>Returns (modifiable) the flag which defines whether the Fix..() methods are allowed to modify geometry of the edges and vertices</description>
        </method>
        <method name="ModifyRemoveLoopMode">
            <description>Returns (modifiable) the flag which defines whether the Fix..() methods are allowed to modify RemoveLoop of the edges</description>
        </method>
        <method name="ClosedWireMode">
            <description>Returns (modifiable) the flag which defines whether the wire is to be closed (by calling methods like FixDegenerated() and FixConnected() for last and first edges).</description>
        </method>
        <method name="PreferencePCurveMode">
            <description>Returns (modifiable) the flag which defines whether the 2d (True) representation of the wire is preferable over 3d one (in the case of ambiguity in FixEdgeCurves).</description>
        </method>
        <method name="FixGapsByRangesMode">
            <description>Returns (modifiable) the flag which defines whether tool tries to fix gaps first by changing curves ranges (i.e. using intersection, extrema, projections) or not.</description>
        </method>
        <method name="FixGaps2dMode">
            <description>Returns (modifiable) the flag for corresponding Fix..() method which defines whether this method will be called from the method APIFix(): -1 default 1 method will be called 0 method will not be called</description>
        </method>
        <method name="FixNonAdjacentIntersectingEdgesMode">
            <description>Returns (modifiable) the flag for corresponding Fix..() method which defines whether this method will be called from the corresponding Fix..() method of the public level: -1 default 1 method will be called 0 method will not be called</description>
        </method>
        <method name="Perform">
            <description>This method performs all the available fixes. If some fix is turned on or off explicitly by the Fix..Mode() flag, this fix is either called or not depending on that flag. Else (i.e. if flag is default) fix is called depending on the situation: some fixes are not called or are limited if order of edges in the wire is not OK, or depending on modes</description>
        </method>
        <method name="FixReorder_1">
            <description>Performs an analysis and reorders edges in the wire using class WireOrder. Flag &lt;theModeBoth&gt; determines the use of miscible mode if necessary.</description>
        </method>
        <method name="FixSmall_1">
            <description>Applies FixSmall(num) to all edges in the wire</description>
        </method>
        <method name="FixConnected_1">
            <description>Applies FixConnected(num) to all edges in the wire Connection between first and last edges is treated only if flag ClosedMode is True If &lt;prec&gt; is -1 then MaxTolerance() is taken.</description>
        </method>
        <method name="FixEdgeCurves">
            <description>Groups the fixes dealing with 3d and pcurves of the edges. The order of the fixes and the default behaviour are: ShapeFix_Edge::FixReversed2d ShapeFix_Edge::FixRemovePCurve (only if forced) ShapeFix_Edge::FixAddPCurve ShapeFix_Edge::FixRemoveCurve3d (only if forced) ShapeFix_Edge::FixAddCurve3d FixSeam, FixShifted, ShapeFix_Edge::FixSameParameter</description>
        </method>
        <method name="FixDegenerated_1">
            <description>Applies FixDegenerated(num) to all edges in the wire Connection between first and last edges is treated only if flag ClosedMode is True</description>
        </method>
        <method name="FixSelfIntersection">
            <description>Applies FixSelfIntersectingEdge(num) and FixIntersectingEdges(num) to all edges in the wire and FixIntersectingEdges(num1, num2) for all pairs num1 and num2 such that num2 &gt;= num1 + 2 and removes wrong edges if any</description>
        </method>
        <method name="FixLacking_1">
            <description>Applies FixLacking(num) to all edges in the wire Connection between first and last edges is treated only if flag ClosedMode is True If &lt;force&gt; is False (default), test for connectness is done with precision of vertex between edges, else it is done with minimal value of vertex tolerance and Analyzer.Precision(). Hence, &lt;force&gt; will lead to inserting lacking edges in replacement of vertices which have big tolerances.</description>
        </method>
        <method name="FixClosed">
            <description>Fixes a wire to be well closed It performs FixConnected, FixDegenerated and FixLacking between last and first edges (independingly on flag ClosedMode and modes for these fixings) If &lt;prec&gt; is -1 then MaxTolerance() is taken.</description>
        </method>
        <method name="FixGaps3d">
            <description>Fixes gaps between ends of 3d curves on adjacent edges myPrecision is used to detect the gaps.</description>
        </method>
        <method name="FixGaps2d">
            <description>Fixes gaps between ends of pcurves on adjacent edges myPrecision is used to detect the gaps.</description>
        </method>
        <method name="FixReorder_2">
            <description>Reorder edges in the wire as determined by WireOrder that should be filled and computed before</description>
        </method>
        <method name="FixSmall_2">
            <description>Fixes Null Length Edge to be removed If an Edge has Null Length (regarding preci, or &lt;precsmall&gt; - what is smaller), it should be removed It can be with no problem if its two vertices are the same Else, if lockvtx is False, it is removed and its end vertex is put on the preceding edge But if lockvtx is True, this edge must be kept ...</description>
        </method>
        <method name="FixConnected_2">
            <description>Fixes connected edges (preceding and current) Forces Vertices (end of preceding-begin of current) to be the same one Tests with starting preci or, if given greater, &lt;prec&gt; If &lt;prec&gt; is -1 then MaxTolerance() is taken.</description>
        </method>
        <method name="FixSeam">
            <description>Fixes a seam edge A Seam edge has two pcurves, one for forward. one for reversed The forward pcurve must be set as first</description>
        </method>
        <method name="FixShifted">
            <description>Fixes edges which have pcurves shifted by whole parameter range on the closed surface (the case may occur if pcurve of edge was computed by projecting 3d curve, which goes along the seam). It compares each two consequent edges and tries to connect them if distance between ends is near to range of the surface. It also can detect and fix the case if all pcurves are connected, but lie out of parametric bounds of the surface. In addition to FixShifted from ShapeFix_Wire, more sophisticated check of degenerate points is performed, and special cases like sphere given by two meridians are treated.</description>
        </method>
        <method name="FixDegenerated_2">
            <description>Fixes Degenerated Edge Checks an &lt;num-th&gt; edge or a point between &lt;num&gt;th-1 and &lt;num&gt;th edges for a singularity on a supporting surface. If singularity is detected, either adds new degenerated edge (before &lt;num&gt;th), or makes &lt;num&gt;th edge to be degenerated.</description>
        </method>
        <method name="FixLacking_2">
            <description>Fixes Lacking Edge Test if two adjucent edges are disconnected in 2d (while connected in 3d), and in that case either increase tolerance of the vertex or add a new edge (straight in 2d space), in order to close wire in 2d. Returns True if edge was added or tolerance was increased.</description>
        </method>
        <method name="FixGap3d">
            <description>Fixes gap between ends of 3d curves on num-1 and num-th edges. myPrecision is used to detect the gap. If convert is True, converts curves to bsplines to bend.</description>
        </method>
        <method name="FixGap2d">
            <description>Fixes gap between ends of pcurves on num-1 and num-th edges. myPrecision is used to detect the gap. If convert is True, converts pcurves to bsplines to bend.</description>
        </method>
        <method name="StatusRemovedSegment" qualifiers="const">
            <description>Querying the status of performed API fixing procedures Each Status..() methods gives information about the last call to the corresponding Fix..() method of API level: OK : no problems detected; nothing done DONE: some problem(s) was(were) detected and successfully fixed FAIL: some problem(s) cannot be fixed</description>
        </method>
        <method name="LastFixStatus" qualifiers="const">
            <description>Queries the status of last call to methods Fix... of advanced level For details see corresponding methods; universal statuses are: OK : problem not detected; nothing done DONE: problem was detected and successfully fixed FAIL: problem cannot be fixed</description>
        </method>
        <method name="FixEdgeTool" qualifiers="const">
            <description>Returns tool for fixing wires.</description>
        </method>
    </methods>
</class>