<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapeextendWiredata" inherits="RefCounted">
    <brief_description>This class provides a data structure necessary for work with the wire as with ordered list of edges, what is required for many algorithms. The advantage of this class is that it allows to work with wires which are not correct. The object of the class ShapeExtend_WireData can be initialized by TopoDS_Wire, and converted back to TopoDS_Wire. An edge in the wire is defined by its rank number. Operations of accessing, adding and removing edge at the given rank number are provided. On the whole wire, operations of circular permutation and reversing (both orientations of all edges and order of edges) are provided as well. This class also provides a method to check if the edge in the wire is a seam (if the wire lies on a face). This class is handled by reference. Such an approach gives the following advantages: 1. Sharing the object of this class strongly optimizes the processes of analysis and fixing performed in parallel on the wire stored in the form of this class. Fixing tool (e.g. ShapeFix_Wire) fixes problems one by one using analyzing tool (e.g. ShapeAnalysis_Wire). Sharing allows not to reinitialize each time the analyzing tool with modified ShapeExtend_WireData what consumes certain time. 2. No copying of contents. The object of ShapeExtend_WireData class has quite big size, returning it as a result of the function would cause additional copying of contents if this class were one handled by value. Moreover, this class is stored as a field in other classes which are they returned as results of functions, storing only a handle to ShapeExtend_WireData saves time and memory.</brief_description>
    <description>! This class provides a data structure necessary for work with the wire as with
! ordered list of edges, what is required for many algorithms. The advantage of
! this class is that it allows to work with wires which are not correct.
! The object of the class ShapeExtend_WireData can be initialized by
! TopoDS_Wire, and converted back to TopoDS_Wire.
! An edge in the wire is defined by its rank number. Operations of accessing,
! adding and removing edge at the given rank number are provided. On the whole
! wire, operations of circular permutation and reversing (both orientations of
! all edges and order of edges) are provided as well.
! This class also provides a method to check if the edge in the wire is a seam
! (if the wire lies on a face).
! This class is handled by reference. Such an approach gives the following advantages:
! 1.    Sharing the object of this class strongly optimizes the processes of
! analysis and fixing performed in parallel on the wire stored in the form
! of this class. Fixing tool (e.g. ShapeFix_Wire) fixes problems one by
! one using analyzing tool (e.g. ShapeAnalysis_Wire). Sharing allows not
! to reinitialize each time the analyzing tool with modified
! ShapeExtend_WireData what consumes certain time.
! 2.    No copying of contents. The object of ShapeExtend_WireData class has
! quite big size, returning it as a result of the function would cause
! additional copying of contents if this class were one handled by value.
! Moreover, this class is stored as a field in other classes which are
! they returned as results of functions, storing only a handle to
! ShapeExtend_WireData saves time and memory.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Empty constructor, creates empty wire with no edges</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Constructor initializing the data from TopoDS_Wire. Calls Init(wire,chained).</description>
        </method>
        <method name="Init_1">
            <description>Copies data from another WireData</description>
        </method>
        <method name="Init_2">
            <description>Loads an already existing wire If &lt;chained&gt; is True (default), edges are added in the sequence as they are explored by TopoDS_Iterator Else, if &lt;chained&gt; is False, wire is explored by BRepTools_WireExplorer and it is guaranteed that edges will be sequentially connected. Remark : In the latter case it can happen that not all edges will be found (because of limitations of BRepTools_WireExplorer for disconnected wires and wires with seam edges).</description>
        </method>
        <method name="Clear">
            <description>Clears data about Wire.</description>
        </method>
        <method name="ComputeSeams">
            <description>Computes the list of seam edges By default (direct call), computing is enforced For indirect call (from IsSeam) it is redone only if not yet already done or if the list of edges has changed Remark : A Seam Edge is an Edge present twice in the list, once as FORWARD and once as REVERSED Each sense has its own PCurve, the one for FORWARD must be set in first</description>
        </method>
        <method name="SetLast">
            <description>Does a circular permutation in order to set &lt;num&gt;th edge last</description>
        </method>
        <method name="SetDegeneratedLast">
            <description>When the wire contains at least one degenerated edge, sets it as last one Note : It is useful to process pcurves, for instance, while the pcurve of a DGNR may not be computed from its 3D part (there is none) it is computed after the other edges have been computed and chained.</description>
        </method>
        <method name="Add_1">
            <description>Adds an edge to a wire, being defined (not yet ended) This is the plain, basic, function to add an edge &lt;num&gt; = 0 (D): Appends at end &lt;num&gt; = 1: Preprends at start else, Insert before &lt;num&gt; Remark : Null Edge is simply ignored</description>
        </method>
        <method name="Add_2">
            <description>Adds an entire wire, considered as a list of edges Remark : The wire is assumed to be ordered (TopoDS_Iterator is used)</description>
        </method>
        <method name="Add_3">
            <description>Adds a wire in the form of WireData</description>
        </method>
        <method name="Add_4">
            <description>Adds an edge or a wire invoking corresponding method Add</description>
        </method>
        <method name="AddOriented_1">
            <description>Adds an edge to start or end of &lt;me&gt;, according to &lt;mode&gt; 0: at end, as direct 1: at end, as reversed 2: at start, as direct 3: at start, as reversed &lt; 0: no adding</description>
        </method>
        <method name="AddOriented_2">
            <description>Adds a wire to start or end of &lt;me&gt;, according to &lt;mode&gt; 0: at end, as direct 1: at end, as reversed 2: at start, as direct 3: at start, as reversed &lt; 0: no adding</description>
        </method>
        <method name="AddOriented_3">
            <description>Adds an edge or a wire invoking corresponding method AddOriented</description>
        </method>
        <method name="Remove">
            <description>Removes an Edge, given its rank. By default removes the last edge.</description>
        </method>
        <method name="Set">
            <description>Replaces an edge at the given rank number &lt;num&gt; with new one. Default is last edge (&lt;num&gt; = 0).</description>
        </method>
        <method name="Reverse_1">
            <description>Reverses the sense of the list and the orientation of each Edge This method should be called when either wire has no seam edges or face is not available</description>
        </method>
        <method name="Reverse_2">
            <description>Reverses the sense of the list and the orientation of each Edge The face is necessary for swapping pcurves for seam edges (first pcurve corresponds to orientation FORWARD, and second to REVERSED; when edge is reversed, pcurves must be swapped) If face is NULL, no swapping is performed</description>
        </method>
        <method name="NbEdges" qualifiers="const">
            <description>Returns the count of currently recorded edges</description>
        </method>
        <method name="NbNonManifoldEdges" qualifiers="const">
            <description>Returns the count of currently recorded non-manifold edges</description>
        </method>
        <method name="NonmanifoldEdge" qualifiers="const">
            <description>Returns &lt;num&gt;th nonmanifold Edge</description>
        </method>
        <method name="NonmanifoldEdges" qualifiers="const">
            <description>Returns sequence of non-manifold edges This sequence can be not empty if wire data set in manifold mode but initial wire has INTERNAL orientation or contains INTERNAL edges</description>
        </method>
        <method name="ManifoldMode">
            <description>Returns mode defining manifold wire data or not. If manifold that nonmanifold edges will not be not consider during operations(previous behaviour) and they will be added only in result wire else non-manifold edges will consider during operations</description>
        </method>
        <method name="Edge" qualifiers="const">
            <description>Returns &lt;num&gt;th Edge</description>
        </method>
        <method name="Index">
            <description>Returns the index of the edge If the edge is a seam the orientation is also checked Returns 0 if the edge is not found in the list</description>
        </method>
        <method name="IsSeam">
            <description>Tells if an Edge is seam (see ComputeSeams) An edge is considered as seam if it presents twice in the edge list, once as FORWARD and once as REVERSED.</description>
        </method>
        <method name="Wire" qualifiers="const">
            <description>Makes TopoDS_Wire using BRep_Builder (just creates the TopoDS_Wire object and adds all edges into it). This method should be called when the wire is correct (for example, after successful fixes by ShapeFix_Wire) and adjacent edges share common vertices. In case if adjacent edges do not share the same vertices the resulting TopoDS_Wire will be invalid.</description>
        </method>
        <method name="WireAPIMake" qualifiers="const">
            <description>Makes TopoDS_Wire using BRepAPI_MakeWire. Class BRepAPI_MakeWire merges geometrically coincided vertices and can disturb correct order of edges in the wire. If this class fails, null shape is returned.</description>
        </method>
    </methods>
</class>