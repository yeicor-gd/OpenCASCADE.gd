<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTransferResultfromtransient" inherits="RefCounted">
    <brief_description>This class, in conjunction with ResultFromModel, allows to record the result of a transfer initially stored in a TransientProcess.</brief_description>
    <description>! This class, in conjunction with ResultFromModel, allows to
! record the result of a transfer initially stored in a
! TransientProcess.
!
! A ResultFromTransient records a couple (Transient,Binder for
! the result and checks) plus a list of "sub-results", which
! have been recorded in the TrabsientProcess, under scope
! attached to the starting transient.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Creates a ResultFromTransient, empty</description>
        </method>
        <method name="SetStart">
            <description>Sets starting entity</description>
        </method>
        <method name="SetBinder">
            <description>Sets Binder (for result plus individual check)</description>
        </method>
        <method name="Start" qualifiers="const">
            <description>Returns the starting entity</description>
        </method>
        <method name="Binder" qualifiers="const">
            <description>Returns the binder</description>
        </method>
        <method name="HasResult" qualifiers="const">
            <description>Returns True if a result is recorded</description>
        </method>
        <method name="Check" qualifiers="const">
            <description>Returns the check (or an empty one if no binder)</description>
        </method>
        <method name="CheckStatus" qualifiers="const">
            <description>Returns the check status</description>
        </method>
        <method name="ClearSubs">
            <description>Clears the list of (immediate) sub-results</description>
        </method>
        <method name="AddSubResult">
            <description>Adds a sub-result</description>
        </method>
        <method name="NbSubResults" qualifiers="const">
            <description>Returns the count of recorded sub-results</description>
        </method>
        <method name="SubResult" qualifiers="const">
            <description>Returns a sub-result, given its rank</description>
        </method>
        <method name="ResultFromKey" qualifiers="const">
            <description>Returns the ResultFromTransient attached to a given starting entity (the key). Returns a null handle if not found</description>
        </method>
        <method name="Fill">
            <description>Fills from a TransientProcess, with the starting entity which must have been set before. It works with scopes, calls Fill on each of its sub-results</description>
        </method>
        <method name="Strip">
            <description>Clears some data attached to binders used by TransientProcess, which become useless once the transfer has been done : the list of sub-scoped binders, which is now recorded as sub-results</description>
        </method>
        <method name="FillBack" qualifiers="const">
            <description>Fills back a TransientProcess with definition of a ResultFromTransient, respectfully to its structuration in scopes</description>
        </method>
    </methods>
</class>