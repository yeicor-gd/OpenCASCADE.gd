<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTransferTransientprocess" inherits="RefCounted">
    <brief_description>Adds specific features to the generic definition : TransientProcess is intended to work from an InterfaceModel to a set of application objects.</brief_description>
    <description>! Adds specific features to the generic definition :
! TransientProcess is intended to work from an InterfaceModel
! to a set of application objects.
!
! Hence, some information about starting entities can be gotten
! from the model : for Trace, CheckList, Integrity Status</description>
    <methods>
        <method name="Transfer_TransientProcess" qualifiers="static">
            <description>Sets TransientProcess at initial state, with an initial size</description>
        </method>
        <method name="SetModel">
            <description>Sets an InterfaceModel, used by StartTrace, CheckList, queries on Integrity, to give information significant for each norm.</description>
        </method>
        <method name="Model" qualifiers="const">
            <description>Returns the Model used for StartTrace</description>
        </method>
        <method name="SetGraph">
            <description>Sets a Graph : supersedes SetModel if already done</description>
        </method>
        <method name="SetContext">
            <description>Sets a Context : according to receiving appli, to be interpreted by the Actor</description>
        </method>
        <method name="GetContext" qualifiers="const">
            <description>Returns the Context attached to a name, if set and if it is Kind of the type, else a Null Handle Returns True if OK, False if no Context</description>
        </method>
        <method name="PrintTrace" qualifiers="const">
            <description>Specific printing to trace an entity : prints label and type (if model is set)</description>
        </method>
        <method name="CheckNum" qualifiers="const">
            <description>Specific number of a starting object for check-list : Number in model</description>
        </method>
        <method name="IsDataLoaded" qualifiers="const">
            <description>Tells if an entity is well loaded from file (even if its data fail on checking, they are present). Mostly often, answers True. Else, there was a syntactic error in the file. A non-loaded entity MAY NOT BE transferred, unless its Report (in the model) is interpreted</description>
        </method>
        <method name="IsDataFail" qualifiers="const">
            <description>Tells if an entity fails on data checking (load time, syntactic, or semantic check). Normally, should answer False. It is not prudent to try transferring an entity which fails on data checking</description>
        </method>
        <method name="PrintStats" qualifiers="const">
            <description>Prints statistics on a given output, according mode</description>
        </method>
    </methods>
</class>