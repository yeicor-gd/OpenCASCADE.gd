<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGceMakehypr" inherits="RefCounted">
    <brief_description>This class implements construction algorithms for `gp_Hypr`. Supported constructions include: - hyperbola from center and two points (one on major axis); - hyperbola from local coordinate system and radii.</brief_description>
    <description>! This class implements construction algorithms for `gp_Hypr`.
! Supported constructions include:
! - hyperbola from center and two points (one on major axis);
! - hyperbola from local coordinate system and radii.
!
! ^YAxis
! |
! FirstConjugateBranch
! |
! Other            |                Main
! --------------------- C ------------------------------&gt;XAxis
! Branch           |                Branch
! |
! |
! SecondConjugateBranch
! |
!
! The local Cartesian coordinate system of the hyperbola is an
! axis placement (two axes).
!
! The "XDirection" and the "YDirection" of the axis placement
! define the plane of the hyperbola.
!
! The "Direction" of the axis placement defines the normal axis
! to the hyperbola's plane.
!
! The "XAxis" of the hyperbola ("Location", "XDirection") is the
! major axis and the "YAxis" of the hyperbola ("Location",
! "YDirection") is the minor axis.
!
! @note The major radius (on major axis) can be lower than the
!       minor radius (on minor axis).</description>
    <methods>
        <method name="from_1" qualifiers="static">
            <description>Creates a hyperbola from a local coordinate system and radii.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Creates a hyperbola from center and two points.</description>
        </method>
        <method name="Value" qualifiers="const">
            <description>Returns the constructed hyperbola. Exceptions StdFail_NotDone if no hyperbola is constructed.</description>
        </method>
        <method name="Operator" qualifiers="const">
            <description>Alias for Value() returning a copy.</description>
        </method>
    </methods>
</class>