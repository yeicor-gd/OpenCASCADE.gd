<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGraphic3dSequenceofhclipplane" inherits="RefCounted">
    <brief_description>Class defines a Clipping Volume as a logical OR (disjunction) operation between Graphic3d_ClipPlane in sequence. Each Graphic3d_ClipPlane represents either a single Plane clipping a halfspace (direction is specified by normal), or a sub-chain of planes defining a logical AND (conjunction) operation. Therefore, this collection allows defining a Clipping Volume through the limited set of Boolean operations between clipping Planes.</brief_description>
    <description>! Class defines a Clipping Volume as a logical OR (disjunction) operation between
! Graphic3d_ClipPlane in sequence. Each Graphic3d_ClipPlane represents either a single Plane
! clipping a halfspace (direction is specified by normal), or a sub-chain of planes defining a
! logical AND (conjunction) operation. Therefore, this collection allows defining a Clipping
! Volume through the limited set of Boolean operations between clipping Planes.
!
! The Clipping Volume can be assigned either to entire View or to a specific Object;
! in the latter case property ToOverrideGlobal() will specify if Object planes should override
! (suppress) globally defined ones or extend their definition through logical OR (disjunction)
! operation.
!
! Note that defining (many) planes will lead to performance degradation, and Graphics Driver may
! limit the overall number of simultaneously active clipping planes - but at least 6 planes should
! be supported on all configurations.</description>
    <methods>
        <method name="Graphic3d_SequenceOfHClipPlane" qualifiers="static">
            <description>Empty constructor.</description>
        </method>
        <method name="ToOverrideGlobal" qualifiers="const">
            <description>Return true if local properties should override global properties.</description>
        </method>
        <method name="SetOverrideGlobal">
            <description>Setup flag defining if local properties should override global properties.</description>
        </method>
        <method name="IsEmpty" qualifiers="const">
            <description>Return TRUE if sequence is empty.</description>
        </method>
        <method name="Size" qualifiers="const">
            <description>Return the number of items in sequence.</description>
        </method>
        <method name="Append">
            <description>Append a plane.</description>
        </method>
        <method name="Remove_1">
            <description>Remove a plane.</description>
        </method>
        <method name="Clear">
            <description>Clear the items out.</description>
        </method>
        <method name="First" qualifiers="const">
            <description>Return the first item in sequence.</description>
        </method>
        <method name="DumpJson" qualifiers="const">
            <description>Dumps the content of me into the stream</description>
        </method>
    </methods>
</class>