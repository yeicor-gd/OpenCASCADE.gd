<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBreptoolsPurgelocations" inherits="RefCounted">
    <brief_description>Removes location datums, which satisfy conditions: aTrsf.IsNegative() || (Abs(Abs(aTrsf.ScaleFactor()) - 1.) &gt; TopLoc_Location::ScalePrec()) from all locations of shape and its subshapes</brief_description>
    <description>! Removes location datums, which satisfy conditions:
! aTrsf.IsNegative() || (Abs(Abs(aTrsf.ScaleFactor()) - 1.) &gt; TopLoc_Location::ScalePrec())
! from all locations of shape and its subshapes</description>
    <methods>
        <method name="Perform">
            <description>Removes all locations correspondingly to criterium from theShape.</description>
        </method>
        <method name="GetResult" qualifiers="const">
            <description>Returns shape with removed locations.</description>
        </method>
        <method name="ModifiedShape" qualifiers="const">
            <description>Returns modified shape obtained from initial shape.</description>
        </method>
    </methods>
</class>