<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBrepbuilderapiFastsewing" inherits="RefCounted">
    <brief_description>This class performs fast sewing of surfaces (faces). It supposes that all surfaces are finite and are naturally restricted by their bounds. Moreover, it supposes that stitched together surfaces have the same parameterization along common boundaries, therefore it does not perform time-consuming check for SameParameter property of edges.</brief_description>
    <description>! This class performs fast sewing of surfaces (faces). It supposes
! that all surfaces are finite and are naturally restricted by their bounds.
! Moreover, it supposes that stitched together surfaces have the same parameterization
! along common boundaries, therefore it does not perform time-consuming check for
! SameParameter property of edges.
!
! For sewing, use this function as following:
! - set tolerance value (default tolerance is 1.E-06)
! - add all necessary surfaces (faces)
! - check status if adding is correctly completed.
! - compute -&gt; Perform
! - retrieve the error status if any
! - retrieve the resulted shape</description>
    <methods>
        <method name="from_double" qualifiers="static">
            <description>Creates an object with tolerance of connexity</description>
        </method>
        <method name="Add_1">
            <description>Adds faces of a shape</description>
        </method>
        <method name="Add_2">
            <description>Adds a surface</description>
        </method>
        <method name="Perform">
            <description>Compute resulted shape</description>
        </method>
        <method name="SetTolerance">
            <description>Sets tolerance</description>
        </method>
        <method name="GetResult" qualifiers="const">
            <description>Returns resulted shape</description>
        </method>
        <method name="GetStatuses">
            <description>Returns list of statuses. Print message if theOS != 0</description>
        </method>
    </methods>
</class>