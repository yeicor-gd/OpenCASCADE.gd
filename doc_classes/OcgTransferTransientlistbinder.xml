<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTransferTransientlistbinder" inherits="RefCounted">
    <brief_description>This binder binds several (a list of) Transients with a starting entity, when this entity itself corresponds to a simple list of Transients. Each part is not seen as a sub-result of an independent component, but as an item of a built-in list</brief_description>
    <description>! This binder binds several (a list of) Transients with a starting
! entity, when this entity itself corresponds to a simple list
! of Transients. Each part is not seen as a sub-result of an
! independent component, but as an item of a built-in list</description>
    <methods>
        <method name="AddResult">
            <description>Adds an item to the result list</description>
        </method>
        <method name="SetResult">
            <description>Changes an already defined sub-result</description>
        </method>
    </methods>
</class>