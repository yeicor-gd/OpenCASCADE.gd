<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBrepoffsetapiMakepipe" inherits="RefCounted">
    <brief_description>Describes functions to build pipes. A pipe is built a basis shape (called the profile) along a wire (called the spine) by sweeping. The profile must not contain solids. A MakePipe object provides a framework for: - defining the construction of a pipe, - implementing the construction algorithm, and - consulting the result. Warning The MakePipe class implements pipe constructions with G1 continuous spines only.</brief_description>
    <description>! Describes functions to build pipes.
! A pipe is built a basis shape (called the profile) along
! a wire (called the spine) by sweeping.
! The profile must not contain solids.
! A MakePipe object provides a framework for:
! - defining the construction of a pipe,
! - implementing the construction algorithm, and
! - consulting the result.
! Warning
! The MakePipe class implements pipe constructions
! with G1 continuous spines only.</description>
    <methods>
        <method name="from_1" qualifiers="static">
            <description>Constructs a pipe by sweeping the shape Profile along the wire Spine.The angle made by the spine with the profile is maintained along the length of the pipe. Warning Spine must be G1 continuous; that is, on the connection vertex of two edges of the wire, the tangent vectors on the left and on the right must have the same direction, though not necessarily the same magnitude. Exceptions Standard_DomainError if the profile is a solid or a composite solid.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>the same as previous but with setting of mode of sweep and the flag that indicates attempt to approximate a C1-continuous surface if a swept surface proved to be C0.</description>
        </method>
        <method name="Build">
            <description>Builds the resulting shape (redefined from MakeShape).</description>
        </method>
        <method name="FirstShape">
            <description>Returns the TopoDS Shape of the bottom of the prism.</description>
        </method>
        <method name="LastShape">
            <description>Returns the TopoDS Shape of the top of the prism.</description>
        </method>
    </methods>
</class>