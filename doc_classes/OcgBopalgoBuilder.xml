<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBopalgoBuilder" inherits="RefCounted">
    <brief_description>The class is a General Fuse algorithm - base algorithm for the algorithms in the Boolean Component. Its main purpose is to build the split parts of the argument shapes from which the result of the operations is combined. The result of the General Fuse algorithm itself is a compound containing all split parts of the arguments.</brief_description>
    <description>!
! The class is a General Fuse algorithm - base algorithm for the
! algorithms in the Boolean Component. Its main purpose is to build
! the split parts of the argument shapes from which the result of
! the operations is combined.&lt;br&gt;
! The result of the General Fuse algorithm itself is a compound
! containing all split parts of the arguments. &lt;br&gt;
!
! Additionally to the options of the base classes, the algorithm has
! the following options:&lt;br&gt;
! - *Safe processing mode* - allows to avoid modification of the input
!                            shapes during the operation (by default it is off);&lt;br&gt;
! - *Gluing options* - allows to speed up the calculation of the intersections
!                      on the special cases, in which some sub-shapes are coinciding.&lt;br&gt;
! - *Disabling the check for inverted solids* - Disables/Enables the check of the input solids
!                          for inverted status (holes in the space). The default value is TRUE,
!                          i.e. the check is performed. Setting this flag to FALSE for inverted
!                          solids, most likely will lead to incorrect results.
!
! The algorithm returns the following warnings:
! - *BOPAlgo_AlertUnableToOrientTheShape* - in case the check on the orientation of the split
! shape
!                                           to match the orientation of the original shape has
!                                           failed.
!
! The algorithm returns the following Error statuses:
! - *BOPAlgo_AlertTooFewArguments* - in case there are no enough arguments to perform the
! operation;
! - *BOPAlgo_AlertNoFiller* - in case the intersection tool has not been created;
! - *BOPAlgo_AlertIntersectionFailed* - in case the intersection of the arguments has failed;
! - *BOPAlgo_AlertBuilderFailed* - in case building splits of arguments has failed with some
! unexpected error.
!</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Empty constructor.</description>
        </method>
        <method name="Clear">
            <description>Clears the content of the algorithm.</description>
        </method>
        <method name="Context" qualifiers="const">
            <description>Returns the Context, tool for cashing heavy algorithms.</description>
        </method>
        <method name="AddArgument">
            <description>Adds the argument to the operation.</description>
        </method>
        <method name="SetArguments">
            <description>Sets the list of arguments for the operation.</description>
        </method>
        <method name="Arguments" qualifiers="const">
            <description>Returns the list of arguments.</description>
        </method>
        <method name="SetNonDestructive">
            <description>Sets the flag that defines the mode of treatment. In non-destructive mode the argument shapes are not modified. Instead a copy of a sub-shape is created in the result if it is needed to be updated. This flag is taken into account if internal PaveFiller is used only. In the case of calling PerformWithFiller the corresponding flag of that PaveFiller is in force.</description>
        </method>
        <method name="NonDestructive" qualifiers="const">
            <description>Returns the flag that defines the mode of treatment. In non-destructive mode the argument shapes are not modified. Instead a copy of a sub-shape is created in the result if it is needed to be updated.</description>
        </method>
        <method name="SetGlue">
            <description>Sets the glue option for the algorithm</description>
        </method>
        <method name="Glue" qualifiers="const">
            <description>Returns the glue option of the algorithm</description>
        </method>
        <method name="SetCheckInverted">
            <description>Enables/Disables the check of the input solids for inverted status</description>
        </method>
        <method name="CheckInverted" qualifiers="const">
            <description>Returns the flag defining whether the check for input solids on inverted status should be performed or not.</description>
        </method>
        <method name="Perform">
            <description>Performs the operation. The intersection will be performed also.</description>
        </method>
        <method name="BuildBOP_2">
            <description>Builds the result shape according to the given states for the objects and tools. These states can be unambiguously converted into the Boolean operation type. Thus, it performs the Boolean operation on the given groups of shapes.</description>
        </method>
        <method name="BuildBOP_1">
            <description>Builds the result of Boolean operation of given type basing on the result of Builder operation (GF or any other).</description>
        </method>
        <method name="Images" qualifiers="const">
            <description>Returns the map of images.</description>
        </method>
        <method name="Origins" qualifiers="const">
            <description>Returns the map of origins.</description>
        </method>
        <method name="ShapesSD" qualifiers="const">
            <description>Returns the map of Same Domain (SD) shapes - coinciding shapes from different arguments.</description>
        </method>
    </methods>
</class>