<?xml version='1.0' encoding='utf-8'?>
<class name="OcgXscontrolController" inherits="RefCounted">
    <brief_description>This class allows a general X-STEP engine to run generic functions on any interface norm, in the same way. It includes the transfer operations. I.e. it gathers the already available general modules, the engine has just to know it</brief_description>
    <description>! This class allows a general X-STEP engine to run generic
! functions on any interface norm, in the same way. It includes
! the transfer operations. I.e. it gathers the already available
! general modules, the engine has just to know it
!
! The important point is that a given X-STEP Controller is
! attached to a given couple made of an Interface Norm (such as
! IGES-5.1) and an application data model (CasCade Shapes for
! instance).
!
! Finally, Controller can be gathered in a general dictionary then
! retrieved later by a general call (method Recorded)
!
! It does not manage the produced data, but the Actors make the
! link between the norm and the application</description>
    <methods>
        <method name="SetNames">
            <description>Changes names if a name is empty, the formerly set one remains Remark : Does not call Record or AutoRecord</description>
        </method>
        <method name="AutoRecord" qualifiers="const">
            <description>Records &lt;me&gt; is a general dictionary under Short and Long Names (see method Name)</description>
        </method>
        <method name="Record" qualifiers="const">
            <description>Records &lt;me&gt; in a general dictionary under a name Error if &lt;name&gt; already used for another one</description>
        </method>
        <method name="Name" qualifiers="const">
            <description>Returns a name, as given when initializing : rsc = False (D) : True Name attached to the Norm (long name) rsc = True : Name of the resource set (i.e. short name)</description>
        </method>
        <method name="Protocol" qualifiers="const">
            <description>Returns the Protocol attached to the Norm (from field)</description>
        </method>
        <method name="WorkLibrary" qualifiers="const">
            <description>Returns the WorkLibrary attached to the Norm. Remark that it has to be in phase with the Protocol (read from field)</description>
        </method>
        <method name="ActorRead" qualifiers="const">
            <description>Returns the Actor for Read attached to the pair (norm,appli) It can be adapted for data of the input Model, as required Can be read from field then adapted with Model as required</description>
        </method>
        <method name="ActorWrite" qualifiers="const">
            <description>Returns the Actor for Write attached to the pair (norm,appli) Read from field. Can be redefined</description>
        </method>
        <method name="SetModeWrite">
            <description>Sets minimum and maximum values for modetrans (write) Erases formerly recorded bounds and values Actually only for shape Then, for each value a little help can be attached</description>
        </method>
        <method name="SetModeWriteHelp">
            <description>Attaches a short line of help to a value of modetrans (write)</description>
        </method>
        <method name="ModeWriteBounds" qualifiers="const">
            <description>Returns recorded min and max values for modetrans (write) Actually only for shapes Returns True if bounds are set, False else (then, free value)</description>
        </method>
        <method name="IsModeWrite" qualifiers="const">
            <description>Tells if a value of &lt;modetrans&gt; is a good value(within bounds) Actually only for shapes</description>
        </method>
        <method name="ModeWriteHelp" qualifiers="const">
            <description>Returns the help line recorded for a value of modetrans empty if help not defined or not within bounds or if values are free</description>
        </method>
        <method name="RecognizeWriteTransient" qualifiers="const">
            <description>Tells if &lt;obj&gt; (an application object) is a valid candidate for a transfer to a Model. By default, asks the ActorWrite if known (through a TransientMapper). Can be redefined</description>
        </method>
        <method name="TransferWriteTransient" qualifiers="const">
            <description>Takes one Transient Object and transfers it to an InterfaceModel (already created, e.g. by NewModel) (result is recorded in the model by AddWithRefs) FP records produced results and checks</description>
        </method>
        <method name="RecognizeWriteShape" qualifiers="const">
            <description>Tells if a shape is valid for a transfer to a model Asks the ActorWrite (through a ShapeMapper)</description>
        </method>
        <method name="TransferWriteShape" qualifiers="const">
            <description>Takes one Shape and transfers it to an InterfaceModel (already created, e.g. by NewModel) Default uses ActorWrite; can be redefined as necessary Returned value is a status, as follows : Done OK , Void : No Result , Fail : Fail (e.g. exception) Error : bad conditions , bad model or null model</description>
        </method>
        <method name="AddSessionItem">
            <description>Records a Session Item, to be added for customisation of the Work Session. It must have a specific name. &lt;setapplied&gt; is used if &lt;item&gt; is a GeneralModifier, to decide If set to true, &lt;item&gt; will be applied to the hook list "send". Else, it is not applied to any hook list. Remark : this method is to be called at Create time, the recorded items will be used by Customise Warning : if &lt;name&gt; conflicts, the last recorded item is kept</description>
        </method>
        <method name="SessionItem" qualifiers="const">
            <description>Returns an item given its name to record in a Session If &lt;name&gt; is unknown, returns a Null Handle</description>
        </method>
        <method name="Customise">
            <description>Customises a WorkSession, by adding to it the recorded items (by AddSessionItem)</description>
        </method>
        <method name="Recorded" qualifiers="static">
            <description>Returns the Controller attached to a given name Returns a Null Handle if &lt;name&gt; is unknown</description>
        </method>
    </methods>
</class>