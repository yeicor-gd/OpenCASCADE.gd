<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapefixComposeshell" inherits="RefCounted">
    <brief_description>This class is intended to create a shell from the composite surface (grid of surfaces) and set of wires. It may be either division of the supporting surface of the face, or creating a shape corresponding to face on composite surface which is missing in CAS.CADE but exists in some other systems.</brief_description>
    <description>! This class is intended to create a shell from the composite
! surface (grid of surfaces) and set of wires.
! It may be either division of the supporting surface of the
! face, or creating a shape corresponding to face on composite
! surface which is missing in CAS.CADE but exists in some other
! systems.
!
! It splits (if necessary) original face to several ones by
! splitting lines which are joint lines on a supplied grid of
! surfaces (U- and V- isolines of the  composite surface).
! There are two modes of work, which differ in the way of
! handling faces on periodic surfaces:
!
! - if ClosedMode is False (default), when splitting itself is
! done as if surface were not periodic. The periodicity of the
! underlying surface is taken into account by duplicating splitting
! lines in the periodic direction, as necessary to split all
! the wires (whole parametrical range of a face)
! In this mode, some regularization procedures are performed
! (indexation of split segments by patch numbers), and it is
! expected to be more reliable and robust in case of bad shapes
!
! - if ClosedMode is True, when everything on a periodic surfaces
! is considered as modulo period. This allows to deal with wires
! which are closed in 3d but not in 2d, with wires which may be
! shifted on several periods in 2d etc. However, this mode is
! less reliable since some regularizations do not work for it.
!
! The work is made basing on pcurves of the edges. These pcurves
! should already exist (for example, in the case of division of
! existing face), then they are taken as is. The existing pcurves
! should be assigned to one surface (face) for all edges,
! this surface (face) will be used only for accessing pcurves,
! and it may have any geometry.
!
! All the modifications are recorded in the context tool
! (ShapeBuild_ReShape).</description>
    <methods>
        <method name="ShapeFix_ComposeShell" qualifiers="static">
            <description>Creates empty tool.</description>
        </method>
        <method name="Init">
            <description>Initializes with composite surface, face and precision. Here face defines both set of wires and way of getting pcurves. Precision is used (together with tolerance of edges) for handling subtle cases, such as tangential intersections.</description>
        </method>
        <method name="ClosedMode">
            <description>Returns (modifiable) flag for special 'closed' mode which forces ComposeShell to consider all pcurves on closed surface as modulo period. This can reduce reliability, but allows to deal with wires closed in 3d but open in 2d (missing seam) Default is False</description>
        </method>
        <method name="Perform">
            <description>Performs the work on already loaded data.</description>
        </method>
        <method name="SplitEdges">
            <description>Splits edges in the original shape by grid. This is a part of Perform() which does not produce any resulting shape; the only result is filled context where splittings are recorded.</description>
        </method>
        <method name="Result" qualifiers="const">
            <description>Returns resulting shell or face (or Null shape if not done)</description>
        </method>
        <method name="Status" qualifiers="const">
            <description>Queries status of last call to Perform() OK : nothing done (some kind of error) DONE1: splitting is done, at least one new face created DONE2: splitting is done, several new faces obtained FAIL1: misoriented wire encountered (handled) FAIL2: recoverable parity error FAIL3: edge with no pcurve on supporting face FAIL4: unrecoverable algorithm error (parity check)</description>
        </method>
        <method name="DispatchWires" qualifiers="const">
            <description>Creates new faces from the set of (closed) wires. Each wire is put on corresponding patch in the composite surface, and all pcurves on the initial (pseudo)face are reassigned to that surface. If several wires are one inside another, single face is created.</description>
        </method>
        <method name="SetTransferParamTool">
            <description>Sets tool for transfer parameters from 3d to 2d and vice versa.</description>
        </method>
        <method name="GetTransferParamTool" qualifiers="const">
            <description>Gets tool for transfer parameters from 3d to 2d and vice versa.</description>
        </method>
    </methods>
</class>