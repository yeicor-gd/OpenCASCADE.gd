<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTransferTransferinput" inherits="RefCounted">
    <brief_description>A TransferInput is a Tool which fills an InterfaceModel with the result of the Transfer of CasCade Objects, once determined The Result comes from a TransferProcess, either from Transient (the Complete Result is considered, it must contain only Transient Objects)</brief_description>
    <description>! A TransferInput is a Tool which fills an InterfaceModel with
! the result of the Transfer of CasCade Objects, once determined
! The Result comes from a TransferProcess, either from
! Transient (the Complete Result is considered, it must contain
! only Transient Objects)</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Creates a TransferInput ready to use</description>
        </method>
        <method name="FillModel_1" qualifiers="const">
            <description>Fills an InterfaceModel with the Complete Result of a Transfer stored in a TransientProcess (Starting Objects are Transient) The complete result is exactly added to the model</description>
        </method>
        <method name="FillModel_3" qualifiers="const">
            <description>Fills an InterfaceModel with results of the Transfer recorded in a TransientProcess (Starting Objects are Transient) : Root Result if &lt;roots&gt; is True (Default), Complete Result else The entities added to the model are determined from the result by by adding the referenced entities</description>
        </method>
        <method name="FillModel_2" qualifiers="const">
            <description>Fills an InterfaceModel with the Complete Result of a Transfer stored in a TransientProcess (Starting Objects are Transient) The complete result is exactly added to the model</description>
        </method>
        <method name="FillModel_4" qualifiers="const">
            <description>Fills an InterfaceModel with results of the Transfer recorded in a TransientProcess (Starting Objects are Transient) : Root Result if &lt;roots&gt; is True (Default), Complete Result else The entities added to the model are determined from the result by by adding the referenced entities</description>
        </method>
    </methods>
</class>