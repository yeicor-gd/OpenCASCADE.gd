<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapefixFreebounds" inherits="RefCounted">
    <brief_description>This class is intended to output free bounds of the shape (free bounds are the wires consisting of edges referenced by the only face). For building free bounds it uses ShapeAnalysis_FreeBounds class. This class complements it with the feature to reduce the number of open wires. This reduction is performed with help of connecting several adjacent open wires one to another what can lead to: 1. making an open wire with greater length out of several open wires 2. making closed wire out of several open wires</brief_description>
    <description>! This class is intended to output free bounds of the shape
! (free bounds are the wires consisting of edges referenced by the
! only face).
! For building free bounds it uses ShapeAnalysis_FreeBounds class.
! This class complements it with the feature to reduce the number
! of open wires.
! This reduction is performed with help of connecting several
! adjacent open wires one to another what can lead to:
! 1. making an open wire with greater length out of several
! open wires
! 2. making closed wire out of several open wires
!
! The connecting open wires is performed with a user-given
! tolerance.
!
! When connecting several open wires into one wire their previous
! end vertices are replaced with new connecting vertices. After
! that all the edges in the shape sharing previous vertices inside
! the shape are updated with new vertices. Thus source shape can
! be modified.
!
! Since interface of this class is the same as one of
! ShapeAnalysis_FreeBounds refer to its CDL for details.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Empty constructor</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Builds actual free bounds of the &lt;shape&gt; and connects open wires with tolerance &lt;closetoler&gt;. &lt;shape&gt; should be a compound of shells.</description>
        </method>
        <method name="from_3" qualifiers="static">
            <description>Builds forecasting free bounds of the &lt;shape&gt; and connects open wires with tolerance &lt;closetoler&gt;. &lt;shape&gt; should be a compound of faces. Tolerance &lt;closetoler&gt; should be greater than tolerance &lt;sewtoler&gt; used for initializing sewing analyzer, otherwise connection of open wires is not performed.</description>
        </method>
        <method name="GetClosedWires" qualifiers="const">
            <description>Returns compound of closed wires out of free edges.</description>
        </method>
        <method name="GetOpenWires" qualifiers="const">
            <description>Returns compound of open wires out of free edges.</description>
        </method>
        <method name="GetShape" qualifiers="const">
            <description>Returns modified source shape.</description>
        </method>
    </methods>
</class>