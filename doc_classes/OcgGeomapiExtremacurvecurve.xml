<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGeomapiExtremacurvecurve" inherits="RefCounted">
    <brief_description>Describes functions for computing all the extrema between two 3D curves. An ExtremaCurveCurve algorithm minimizes or maximizes the distance between a point on the first curve and a point on the second curve. Thus, it computes start and end points of perpendiculars common to the two curves (an intersection point is not an extremum unless the two curves are tangential at this point). Solutions consist of pairs of points, and an extremum is considered to be a segment joining the two points of a solution. An ExtremaCurveCurve object provides a framework for: - defining the construction of the extrema, - implementing the construction algorithm, and - consulting the results. Warning In some cases, the nearest points between two curves do not correspond to one of the computed extrema. Instead, they may be given by: - a limit point of one curve and one of the following: - its orthogonal projection on the other curve, - a limit point of the other curve; or - an intersection point between the two curves.</brief_description>
    <description>! Describes functions for computing all the extrema
! between two 3D curves.
! An ExtremaCurveCurve algorithm minimizes or
! maximizes the distance between a point on the first
! curve and a point on the second curve. Thus, it
! computes start and end points of perpendiculars
! common to the two curves (an intersection point is
! not an extremum unless the two curves are tangential at this point).
! Solutions consist of pairs of points, and an extremum
! is considered to be a segment joining the two points of a solution.
! An ExtremaCurveCurve object provides a framework for:
! -   defining the construction of the extrema,
! -   implementing the construction algorithm, and
! -   consulting the results.
! Warning
! In some cases, the nearest points between two
! curves do not correspond to one of the computed
! extrema. Instead, they may be given by:
! -   a limit point of one curve and one of the following:
! -   its orthogonal projection on the other curve,
! -   a limit point of the other curve; or
! -   an intersection point between the two curves.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Constructs an empty algorithm for computing extrema between two curves. Use an Init function to define the curves on which it is going to work.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Computes the extrema between the curves C1 and C2.</description>
        </method>
        <method name="from_3" qualifiers="static">
            <description>Computes the portion of the curve C1 limited by the two points of parameter (U1min,U1max), and - the portion of the curve C2 limited by the two points of parameter (U2min,U2max). Warning Use the function NbExtrema to obtain the number of solutions. If this algorithm fails, NbExtrema returns 0.</description>
        </method>
        <method name="Init_1">
            <description>Initializes this algorithm with the given arguments and computes the extrema between the curves C1 and C2</description>
        </method>
        <method name="Init_2">
            <description>Initializes this algorithm with the given arguments and computes the extrema between : - the portion of the curve C1 limited by the two points of parameter (U1min,U1max), and - the portion of the curve C2 limited by the two points of parameter (U2min,U2max). Warning Use the function NbExtrema to obtain the number of solutions. If this algorithm fails, NbExtrema returns 0.</description>
        </method>
        <method name="NbExtrema" qualifiers="const">
            <description>Returns the number of extrema computed by this algorithm. Note: if this algorithm fails, NbExtrema returns 0.</description>
        </method>
        <method name="Points" qualifiers="const">
            <description>Returns the points P1 on the first curve and P2 on the second curve, which are the ends of the extremum of index Index computed by this algorithm. Exceptions Standard_OutOfRange if Index is not in the range [ 1,NbExtrema ], where NbExtrema is the number of extrema computed by this algorithm.</description>
        </method>
        <method name="Parameters" qualifiers="const">
            <description>Returns the parameters U1 of the point on the first curve and U2 of the point on the second curve, which are the ends of the extremum of index Index computed by this algorithm. Exceptions Standard_OutOfRange if Index is not in the range [ 1,NbExtrema ], where NbExtrema is the number of extrema computed by this algorithm.</description>
        </method>
        <method name="Distance" qualifiers="const">
            <description>Computes the distance between the end points of the extremum of index Index computed by this algorithm. Exceptions Standard_OutOfRange if Index is not in the range [ 1,NbExtrema ], where NbExtrema is the number of extrema computed by this algorithm.</description>
        </method>
        <method name="IsParallel" qualifiers="const">
            <description>Returns True if the two curves are parallel.</description>
        </method>
        <method name="NearestPoints" qualifiers="const">
            <description>Returns the points P1 on the first curve and P2 on the second curve, which are the ends of the shortest extremum computed by this algorithm. Exceptions StdFail_NotDone if this algorithm fails.</description>
        </method>
        <method name="LowerDistanceParameters" qualifiers="const">
            <description>Returns the parameters U1 of the point on the first curve and U2 of the point on the second curve, which are the ends of the shortest extremum computed by this algorithm. Exceptions StdFail_NotDone if this algorithm fails.</description>
        </method>
        <method name="LowerDistance" qualifiers="const">
            <description>Computes the distance between the end points of the shortest extremum computed by this algorithm. Exceptions StdFail_NotDone if this algorithm fails.</description>
        </method>
        <method name="TotalNearestPoints">
            <description>set in &lt;P1&gt; and &lt;P2&gt; the couple solution points such a the distance [P1,P2] is the minimum. taking in account extremity points of curves.</description>
        </method>
        <method name="TotalLowerDistanceParameters">
            <description>set in &lt;U1&gt; and &lt;U2&gt; the parameters of the couple solution points which represents the total nearest solution.</description>
        </method>
        <method name="TotalLowerDistance">
            <description>return the distance of the total nearest couple solution point. if &lt;myExtCC&gt; is not done</description>
        </method>
    </methods>
</class>