<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGeom2dOffsetcurve" inherits="RefCounted">
    <brief_description>This class implements the basis services for the creation, edition, modification and evaluation of planar offset curve. The offset curve is obtained by offsetting by distance along the normal to a basis curve defined in 2D space. The offset curve in this package can be a self intersecting curve even if the basis curve does not self-intersect. The self intersecting portions are not deleted at the construction time. An offset curve is a curve at constant distance (Offset) from a basis curve and the offset curve takes its parametrization from the basis curve. The Offset curve is in the direction of the normal to the basis curve N. The distance offset may be positive or negative to indicate the preferred side of the curve : . distance offset &gt;0 =&gt; the curve is in the direction of N . distance offset &gt;0 =&gt; the curve is in the direction of - N On the Offset curve : Value(u) = BasisCurve.Value(U) + (Offset * (T ^ Z)) / ||T ^ Z|| where T is the tangent vector to the basis curve and Z the direction of the normal vector to the plane of the curve, N = T ^ Z defines the offset direction and should not have null length.</brief_description>
    <description>! This class implements the basis services for the creation,
! edition, modification and evaluation of planar offset curve.
! The offset curve is obtained by offsetting by distance along
! the normal to a basis curve defined in 2D space.
! The offset curve in this package can be a self intersecting
! curve even if the basis curve does not self-intersect.
! The self intersecting portions are not deleted at the
! construction time.
! An offset curve is a curve at constant distance (Offset) from a
! basis curve and the offset curve takes its parametrization from
! the basis curve. The Offset curve is in the direction of the
! normal to the basis curve N.
! The distance offset may be positive or negative to indicate the
! preferred side of the curve :
! . distance offset &gt;0 =&gt; the curve is in the direction of N
! . distance offset &gt;0 =&gt; the curve is in the direction of - N
! On the Offset curve :
! Value(u) = BasisCurve.Value(U) + (Offset * (T ^ Z)) / ||T ^ Z||
! where T is the tangent vector to the basis curve and Z the
! direction of the normal vector to the plane of the curve,
! N = T ^ Z defines the offset direction and should not have
! null length.
!
! Warnings :
! In this package we suppose that the continuity of the offset
! curve is one degree less than the continuity of the
! basis curve and we don't check that at any point ||T^Z|| != 0.0
!
! So to evaluate the curve it is better to check that the offset
! curve is well defined at any point because an exception could
! be raised. The check is not done in this package at the creation
! of the offset curve because the control needs the use of an
! algorithm which cannot be implemented in this package.
! The OffsetCurve is closed if the first point and the last point
! are the same (The distance between these two points is lower or
! equal to the Resolution sea package gp) . The OffsetCurve can be
! closed even if the basis curve is not closed.</description>
    <methods>
        <method name="Geom2d_OffsetCurve" qualifiers="static">
            <description>Copy constructor for optimized copying without validation.</description>
        </method>
        <method name="ClearEvalRepresentation">
            <description>Removes the evaluation representation.</description>
        </method>
        <method name="Reverse">
            <description>Changes the direction of parametrization of &lt;me&gt;. As a result: - the basis curve is reversed, - the start point of the initial curve becomes the end point of the reversed curve, - the end point of the initial curve becomes the start point of the reversed curve, and - the first and last parameters are recomputed.</description>
        </method>
        <method name="ReversedParameter" qualifiers="const">
            <description>Computes the parameter on the reversed curve for the point of parameter U on this offset curve.</description>
        </method>
        <method name="SetOffsetValue">
            <description>Changes this offset curve by assigning D as the offset value.</description>
        </method>
        <method name="Continuity" qualifiers="const">
            <description>Continuity of the Offset curve : C0 : only geometric continuity, C1 : continuity of the first derivative all along the Curve, C2 : continuity of the second derivative all along the Curve, C3 : continuity of the third derivative all along the Curve, G1 : tangency continuity all along the Curve, G2 : curvature continuity all along the Curve, CN : the order of continuity is infinite. Warnings : Returns the continuity of the basis curve - 1. The offset curve must have a unique normal direction defined at any point. Value and derivatives</description>
        </method>
        <method name="EvalD0" qualifiers="const">
            <description>Warning! this should not be called if the basis curve is not at least C1. Nevertheless if used on portion where the curve is C1, it is OK</description>
        </method>
        <method name="EvalDN" qualifiers="const">
            <description>The returned vector gives the value of the derivative for the order of derivation N. Warning! this should not be called raises UndefunedDerivative if the continuity of the basis curve is not CN+1. Nevertheless, it's OK to use it on portion where the curve is CN+1 raises RangeError if N &lt; 1. raises NotImplemented if N &gt; 3. The following functions compute the value and derivatives on the offset curve and returns the derivatives on the basis curve too. The computation of the value and derivatives on the basis curve are used to evaluate the offset curve Warnings : The exception UndefinedValue or UndefinedDerivative is raised if it is not possible to compute a unique offset direction.</description>
        </method>
        <method name="FirstParameter" qualifiers="const">
            <description>Returns the value of the first parameter of this offset curve. The first parameter corresponds to the start point of the curve. Note: the first and last parameters of this offset curve are also the ones of its basis curve.</description>
        </method>
        <method name="LastParameter" qualifiers="const">
            <description>Returns the value of the last parameter of this offset curve. The last parameter corresponds to the end point. Note: the first and last parameters of this offset curve are also the ones of its basis curve.</description>
        </method>
        <method name="Offset" qualifiers="const">
            <description>Returns the offset value of this offset curve.</description>
        </method>
        <method name="IsClosed" qualifiers="const">
            <description>Returns True if the distance between the start point and the end point of the curve is lower or equal to Resolution from package gp.</description>
        </method>
        <method name="IsCN" qualifiers="const">
            <description>Is the order of continuity of the curve N ? Warnings : This method answer True if the continuity of the basis curve is N + 1. We suppose in this class that a normal direction to the basis curve (used to compute the offset curve) is defined at any point on the basis curve. Raised if N &lt; 0.</description>
        </method>
        <method name="IsPeriodic" qualifiers="const">
            <description>Is the parametrization of a curve is periodic ? If the basis curve is a circle or an ellipse the corresponding OffsetCurve is periodic. If the basis curve can't be periodic (for example BezierCurve) the OffsetCurve can't be periodic.</description>
        </method>
        <method name="Period" qualifiers="const">
            <description>Returns the period of this offset curve, i.e. the period of the basis curve of this offset curve. Exceptions Standard_NoSuchObject if the basis curve is not periodic.</description>
        </method>
        <method name="Transform">
            <description>Applies the transformation T to this offset curve. Note: the basis curve is also modified.</description>
        </method>
        <method name="TransformedParameter" qualifiers="const">
            <description>Returns the parameter on the transformed curve for the transform of the point of parameter U on &lt;me&gt;.</description>
        </method>
        <method name="ParametricTransformation" qualifiers="const">
            <description>Returns a coefficient to compute the parameter on the transformed curve for the transform of the point on &lt;me&gt;.</description>
        </method>
        <method name="GetBasisCurveContinuity" qualifiers="const">
            <description>Returns continuity of the basis curve.</description>
        </method>
        <method name="DumpJson" qualifiers="const">
            <description>Dumps the content of me into the stream</description>
        </method>
    </methods>
</class>