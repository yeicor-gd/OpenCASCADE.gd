<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapeupgradeWiredivide" inherits="RefCounted">
    <brief_description>Divides edges in the wire lying on the face or free wires or free edges with a criterion. Splits 3D curve and pcurve(s) of the edge on the face. Other pcurves which may be associated with the edge are simply copied. If 3D curve is split then pcurve on the face is split as well, and vice-versa. Input shape is not modified. The modifications made are recorded in external context (ShapeBuild_ReShape). This tool is applied to all edges before splitting them in order to keep sharing.</brief_description>
    <description>! Divides edges in the wire lying on the face or free wires or
! free edges with a criterion.
! Splits 3D curve and pcurve(s) of the edge on the face.
! Other pcurves which may be associated with the edge are simply
! copied.
! If 3D curve is split then pcurve on the face is split as
! well, and vice-versa.
! Input shape is not modified.
! The modifications made are recorded in external context
! (ShapeBuild_ReShape). This tool is applied to all edges
! before splitting them in order to keep sharing.</description>
    <methods>
        <method name="ShapeUpgrade_WireDivide" qualifiers="static">
            <description>Empty constructor</description>
        </method>
        <method name="Init">
            <description>Initializes by wire and face</description>
        </method>
        <method name="Load_1">
            <description>Loads working wire</description>
        </method>
        <method name="Load_2">
            <description>Creates wire of one edge and calls Load for wire</description>
        </method>
        <method name="SetFace">
            <description>Sets supporting surface by face</description>
        </method>
        <method name="Perform">
            <description>Computes the resulting wire by splitting all the edges according to splitting criteria. All the modifications made are recorded in context (ShapeBuild_ReShape). This tool is applied to all edges before splitting them in order to keep sharings. If no supporting face or surface is defined, only 3d splitting criteria are used.</description>
        </method>
        <method name="Wire" qualifiers="const">
            <description>Gives the resulting Wire (equal to initial one if not done or Null if not loaded)</description>
        </method>
        <method name="Status" qualifiers="const">
            <description>Queries status of last call to Perform() OK - no edges were split, wire left untouched DONE1 - some edges were split FAIL1 - some edges have no 3d curve (skipped) FAIL2 - some edges have no pcurve (skipped)</description>
        </method>
        <method name="SetEdgeMode">
            <description>Sets mode for splitting 3d curves from edges. 0 - only curve 3d from free edges. 1 - only curve 3d from shared edges. 2 - all curve 3d.</description>
        </method>
    </methods>
</class>