<?xml version='1.0' encoding='utf-8'?>
<class name="OcgXscontrolSigntransferstatus" inherits="RefCounted">
    <brief_description>This Signatures gives the Transfer Status of an entity, as recorded in a TransferProcess. It can be : - Void : not recorded, or recorded as void with no message (attributes are not taken into account) - Warning : no result, warning message(s), no fail - Fail : no result, fail messages (with or without warning) - Result.. : result, no message (neither warning nor fail) Result.. i.e. Result:TypeName of the result - Result../Warning : result, with warning but no fail - Result../Fail : result, with fail (.e. bad result) - Fail on run : no result yet recorded, no message, but an exception occurred while recording the result (this should not appear and indicates a programming error)</brief_description>
    <description>! This Signatures gives the Transfer Status of an entity, as
! recorded in a TransferProcess. It can be :
! - Void : not recorded, or recorded as void with no message
! (attributes are not taken into account)
! - Warning : no result, warning message(s), no fail
! - Fail : no result, fail messages (with or without warning)
! - Result.. : result, no message (neither warning nor fail)
! Result.. i.e. Result:TypeName of the result
! - Result../Warning : result, with warning but no fail
! - Result../Fail : result, with fail (.e. bad result)
! - Fail on run : no result yet recorded, no message, but
! an exception occurred while recording the result
! (this should not appear and indicates a programming error)</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Creates a SignTransferStatus, not initialised it gives nothing (empty string)</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Creates a SignTransferStatus, which will work on the current TransientProcess brought by the TransferReader (its MapReader)</description>
        </method>
        <method name="SetReader">
            <description>Sets a TransferReader to work</description>
        </method>
        <method name="SetMap">
            <description>Sets a precise map to sign entities This definition oversedes the creation with a TransferReader</description>
        </method>
        <method name="Map" qualifiers="const">
            <description>Returns the TransientProcess used as precised one Returns a Null Handle for a creation from a TransferReader without any further setting</description>
        </method>
        <method name="Reader" qualifiers="const">
            <description>Returns the Reader (if created with a Reader) Returns a Null Handle if not created with a Reader</description>
        </method>
        <method name="Value" qualifiers="const">
            <description>Returns the Signature for a Transient object, as its transfer status</description>
        </method>
    </methods>
</class>