<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapefixShapetolerance" inherits="RefCounted">
    <brief_description>Modifies tolerances of sub-shapes (vertices, edges, faces)</brief_description>
    <description>! Modifies tolerances of sub-shapes (vertices, edges, faces)</description>
    <methods>
        <method name="LimitTolerance" qualifiers="const">
            <description>Limits tolerances in a shape as follows : tmin = tmax -&gt; as SetTolerance (forces) tmin = 0 -&gt; maximum tolerance will be &lt;tmax&gt; tmax = 0 or not given (more generally, tmax &lt; tmin) -&gt; &lt;tmax&gt; ignored, minimum will be &lt;tmin&gt; else, maximum will be &lt;max&gt; and minimum will be &lt;min&gt; styp = VERTEX : only vertices are set styp = EDGE : only edges are set styp = FACE : only faces are set styp = WIRE : to have edges and their vertices set styp = other value : all (vertices,edges,faces) are set Returns True if at least one tolerance of the sub-shape has been modified</description>
        </method>
        <method name="SetTolerance" qualifiers="const">
            <description>Sets (enforces) tolerances in a shape to the given value styp = VERTEX : only vertices are set styp = EDGE : only edges are set styp = FACE : only faces are set styp = WIRE : to have edges and their vertices set styp = other value : all (vertices,edges,faces) are set</description>
        </method>
    </methods>
</class>