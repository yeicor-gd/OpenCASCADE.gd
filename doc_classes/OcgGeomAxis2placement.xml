<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGeomAxis2placement" inherits="RefCounted">
    <brief_description>Describes a right-handed coordinate system in 3D space. A coordinate system is defined by: - its origin, also termed the "Location point" of the coordinate system, - three orthogonal unit vectors, termed respectively the "X Direction", "Y Direction" and "Direction" (or "main Direction") of the coordinate system. As a Geom_Axis2Placement coordinate system is right-handed, its "Direction" is always equal to the cross product of its "X Direction" and "Y Direction". The "Direction" of a coordinate system is called the "main Direction" because when this unit vector is modified, the "X Direction" and "Y Direction" are recomputed, whereas when the "X Direction" or "Y Direction" is changed, the "main Direction" is retained. The "main Direction" is also the "Z Direction". Note: Geom_Axis2Placement coordinate systems provide the same kind of "geometric" services as gp_Ax2 coordinate systems but have more complex data structures. The geometric objects provided by the Geom package use gp_Ax2 objects to include coordinate systems in their data structures, or to define the geometric transformations, which are applied to them. Geom_Axis2Placement coordinate systems are used in a context where they can be shared by several objects contained inside a common data structure.</brief_description>
    <description>! Describes a right-handed coordinate system in 3D space.
! A coordinate system is defined by:
! - its origin, also termed the "Location point" of the coordinate system,
! - three orthogonal unit vectors, termed respectively
! the "X Direction", "Y Direction" and "Direction" (or
! "main Direction") of the coordinate system.
! As a Geom_Axis2Placement coordinate system is
! right-handed, its "Direction" is always equal to the
! cross product of its "X Direction" and "Y Direction".
! The "Direction" of a coordinate system is called the
! "main Direction" because when this unit vector is
! modified, the "X Direction" and "Y Direction" are
! recomputed, whereas when the "X Direction" or "Y
! Direction" is changed, the "main Direction" is
! retained. The "main Direction" is also the "Z Direction".
! Note: Geom_Axis2Placement coordinate systems
! provide the same kind of "geometric" services as
! gp_Ax2 coordinate systems but have more complex
! data structures. The geometric objects provided by
! the Geom package use gp_Ax2 objects to include
! coordinate systems in their data structures, or to
! define the geometric transformations, which are applied to them.
! Geom_Axis2Placement coordinate systems are
! used in a context where they can be shared by
! several objects contained inside a common data structure.</description>
    <methods>
        <method name="from_1" qualifiers="static">
            <description>Returns a transient copy of A2.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>P is the origin of the axis placement, N is the main direction of the axis placement and Vx is the "XDirection". If the two directions N and Vx are not orthogonal the "XDirection" is computed as follow : XDirection = N ^ (Vx ^ N). Raised if N and Vx are parallel.</description>
        </method>
        <method name="SetAx2">
            <description>Assigns the origin and the three unit vectors of A2 to this coordinate system.</description>
        </method>
        <method name="SetDirection">
            <description>Changes the main direction of the axis placement. The "Xdirection" is modified : New XDirection = V ^ (Previous_Xdirection ^ V).</description>
        </method>
        <method name="SetXDirection">
            <description>Changes the "XDirection" of the axis placement, Vx is the new "XDirection". If Vx is not normal to the main direction then "XDirection" is computed as follow : XDirection = Direction ^ ( Vx ^ Direction). The main direction is not modified. Raised if Vx and "Direction" are parallel.</description>
        </method>
        <method name="SetYDirection">
            <description>Changes the "YDirection" of the axis placement, Vy is the new "YDirection". If Vy is not normal to the main direction then "YDirection" is computed as follow : YDirection = Direction ^ ( Vy ^ Direction). The main direction is not modified. The "XDirection" is modified. Raised if Vy and the main direction are parallel.</description>
        </method>
        <method name="Ax2" qualifiers="const">
            <description>Returns a non transient copy of &lt;me&gt;.</description>
        </method>
        <method name="XDirection" qualifiers="const">
            <description>Returns the "XDirection". This is a unit vector.</description>
        </method>
        <method name="YDirection" qualifiers="const">
            <description>Returns the "YDirection". This is a unit vector.</description>
        </method>
        <method name="Transform">
            <description>Transforms an axis placement with a Trsf. The "Location" point, the "XDirection" and the "YDirection" are transformed with T. The resulting main "Direction" of &lt;me&gt; is the cross product between the "XDirection" and the "YDirection" after transformation.</description>
        </method>
    </methods>
</class>