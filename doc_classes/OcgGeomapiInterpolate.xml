<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGeomapiInterpolate" inherits="RefCounted">
    <brief_description>This class is used to interpolate a BsplineCurve passing through an array of points, with a C2 Continuity if tangency is not requested at the point. If tangency is requested at the point the continuity will be C1. If Perodicity is requested the curve will be closed and the junction will be the first point given. The curve will than be only C1 Describes functions for building a constrained 3D BSpline curve. The curve is defined by a table of points through which it passes, and if required: - by a parallel table of reals which gives the value of the parameter of each point through which the resulting BSpline curve passes, and - by vectors tangential to these points. An Interpolate object provides a framework for: - defining the constraints of the BSpline curve, - implementing the interpolation algorithm, and - consulting the results.</brief_description>
    <description>! This class is used to interpolate a BsplineCurve
! passing through an array of points, with a C2
! Continuity if tangency is not requested at the point.
! If tangency is requested at the point the continuity will
! be C1. If Perodicity is requested the curve will be closed
! and the junction will be the first point given. The curve
! will than be only C1
! Describes functions for building a constrained 3D BSpline curve.
! The curve is defined by a table of points
! through which it passes, and if required:
! -   by a parallel table of reals which gives the
! value of the parameter of each point through
! which the resulting BSpline curve passes, and
! -   by vectors tangential to these points.
! An Interpolate object provides a framework for:
! -   defining the constraints of the BSpline curve,
! -   implementing the interpolation algorithm, and
! -   consulting the results.</description>
    <methods>
        <method name="Load">
            <description>Assigns this constrained BSpline curve to be tangential to vectors InitialTangent and FinalTangent at its first and last points respectively (i.e. the first and last points of the table of points through which the curve passes, as defined at the time of initialization).</description>
        </method>
        <method name="ClearTangents">
            <description>Clears all tangency constraints on this constrained BSpline curve (as initialized by the function Load).</description>
        </method>
        <method name="Perform">
            <description>Computes the constrained BSpline curve. Use the function IsDone to verify that the computation is successful, and then the function Curve to obtain the result.</description>
        </method>
        <method name="Curve" qualifiers="const">
            <description>Returns the computed BSpline curve. Raises StdFail_NotDone if the interpolation fails.</description>
        </method>
        <method name="IsDone" qualifiers="const">
            <description>Returns true if the constrained BSpline curve is successfully constructed. Note: in this case, the result is given by the function Curve.</description>
        </method>
    </methods>
</class>