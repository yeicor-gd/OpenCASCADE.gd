<?xml version='1.0' encoding='utf-8'?>
<class name="OcgShapeanalysisTransferparameters" inherits="RefCounted">
    <brief_description>This tool is used for transferring parameters from 3d curve of the edge to pcurve and vice versa.</brief_description>
    <description>! This tool is used for transferring parameters
! from 3d curve of the edge to pcurve and vice versa.
!
! Default behaviour is to trsnafer parameters with help
! of linear transformation:
!
! T2d = myShift + myScale * T3d
! where
! myScale = ( Last2d - First2d ) / ( Last3d - First3d )
! myShift = First2d - First3d * myScale
! [First3d, Last3d] and [First2d, Last2d] are ranges of
! edge on curve and pcurve
!
! This behaviour can be redefined in derived classes, for example,
! using projection.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Creates empty tool with myShift = 0 and myScale = 1</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Creates a tool and initializes it with edge and face</description>
        </method>
        <method name="Init">
            <description>Initialize a tool with edge and face</description>
        </method>
        <method name="SetMaxTolerance">
            <description>Sets maximal tolerance to use linear recomputation of parameters.</description>
        </method>
        <method name="Perform_1">
            <description>Transfers parameters given by sequence Params from 3d curve to pcurve (if To2d is True) or back (if To2d is False)</description>
        </method>
        <method name="Perform_2">
            <description>Transfers parameter given by sequence Params from 3d curve to pcurve (if To2d is True) or back (if To2d is False)</description>
        </method>
        <method name="TransferRange">
            <description>Recomputes range of curves from NewEdge. If Is2d equals True parameters are recomputed by curve2d else by curve3d.</description>
        </method>
        <method name="IsSameRange" qualifiers="const">
            <description>Returns True if 3d curve of edge and pcurve are SameRange (in default implementation, if myScale == 1 and myShift == 0)</description>
        </method>
    </methods>
</class>