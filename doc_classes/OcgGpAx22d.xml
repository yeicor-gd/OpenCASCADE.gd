<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGpAx22d" inherits="RefCounted">
    <brief_description>Describes a coordinate system in a plane (2D space). A coordinate system is defined by: - its origin (also referred to as its "Location point"), and - two orthogonal unit vectors, respectively, called the "X Direction" and the "Y Direction". A gp_Ax22d may be right-handed ("direct sense") or left-handed ("inverse" or "indirect sense"). You use a gp_Ax22d to: - describe 2D geometric entities, in particular to position them. The local coordinate system of a geometric entity serves for the same purpose as the STEP function "axis placement two axes", or - define geometric transformations. Note: we refer to the "X Axis" and "Y Axis" as the axes having: - the origin of the coordinate system as their origin, and - the unit vectors "X Direction" and "Y Direction", respectively, as their unit vectors.</brief_description>
    <description>! Describes a coordinate system in a plane (2D space).
! A coordinate system is defined by:
! -   its origin (also referred to as its "Location point"), and
! -   two orthogonal unit vectors, respectively, called the "X
! Direction" and the "Y Direction".
! A gp_Ax22d may be right-handed ("direct sense") or
! left-handed ("inverse" or "indirect sense").
! You use a gp_Ax22d to:
! - describe 2D geometric entities, in particular to position
! them. The local coordinate system of a geometric
! entity serves for the same purpose as the STEP
! function "axis placement two axes", or
! -   define geometric transformations.
! Note: we refer to the "X Axis" and "Y Axis" as the axes having:
! -   the origin of the coordinate system as their origin, and
! -   the unit vectors "X Direction" and "Y Direction",
! respectively, as their unit vectors.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Creates an object representing the reference coordinate system (OXY).</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Creates - a coordinate system where its origin is the origin of theA and its "X Direction" is the unit vector of theA, which is: - right-handed if theIsSense is true (default value), or - left-handed if theIsSense is false.</description>
        </method>
        <method name="from_3" qualifiers="static">
            <description>Creates a coordinate system with origin theP and where: - theVx is the "X Direction", and - the "Y Direction" is orthogonal to theVx and oriented so that the cross products theVx^"Y Direction" and theVx^theVy have the same sign. Raises ConstructionError if theVx and theVy are parallel (same or opposite orientation).</description>
        </method>
        <method name="from_4" qualifiers="static">
            <description>Creates - a coordinate system with origin theP and "X Direction" theV, which is: - right-handed if theIsSense is true (default value), or - left-handed if theIsSense is false</description>
        </method>
        <method name="SetAxis">
            <description>Assigns the origin and the two unit vectors of the coordinate system theA1 to this coordinate system.</description>
        </method>
        <method name="SetXAxis">
            <description>Changes the XAxis and YAxis ("Location" point and "Direction") of &lt;me&gt;. The "YDirection" is recomputed in the same sense as before.</description>
        </method>
        <method name="SetYAxis">
            <description>Changes the XAxis and YAxis ("Location" point and "Direction") of &lt;me&gt;. The "XDirection" is recomputed in the same sense as before.</description>
        </method>
        <method name="SetLocation">
            <description>Changes the "Location" point (origin) of &lt;me&gt;.</description>
        </method>
        <method name="SetXDirection">
            <description>Assigns theVx to the "X Direction" of this coordinate system. The other unit vector of this coordinate system is recomputed, normal to theVx , without modifying the orientation (right-handed or left-handed) of this coordinate system.</description>
        </method>
        <method name="SetYDirection">
            <description>Assignsr theVy to the "Y Direction" of this coordinate system. The other unit vector of this coordinate system is recomputed, normal to theVy, without modifying the orientation (right-handed or left-handed) of this coordinate system.</description>
        </method>
        <method name="XAxis" qualifiers="const">
            <description>Returns an axis, for which - the origin is that of this coordinate system, and - the unit vector is either the "X Direction" of this coordinate system. Note: the result is the "X Axis" of this coordinate system.</description>
        </method>
        <method name="YAxis" qualifiers="const">
            <description>Returns an axis, for which - the origin is that of this coordinate system, and - the unit vector is either the "Y Direction" of this coordinate system. Note: the result is the "Y Axis" of this coordinate system.</description>
        </method>
        <method name="Location" qualifiers="const">
            <description>Returns the "Location" point (origin) of &lt;me&gt;.</description>
        </method>
        <method name="XDirection" qualifiers="const">
            <description>Returns the "XDirection" of &lt;me&gt;.</description>
        </method>
        <method name="YDirection" qualifiers="const">
            <description>Returns the "YDirection" of &lt;me&gt;.</description>
        </method>
        <method name="Mirrored_1" qualifiers="const">
            <description>Performs the symmetrical transformation of an axis placement with respect to the point theP which is the center of the symmetry. Warnings : The main direction of the axis placement is not changed. The "XDirection" and the "YDirection" are reversed. So the axis placement stay right handed.</description>
        </method>
        <method name="Mirrored_2" qualifiers="const">
            <description>Performs the symmetrical transformation of an axis placement with respect to an axis placement which is the axis of the symmetry. The transformation is performed on the "Location" point, on the "XDirection" and "YDirection". The resulting main "Direction" is the cross product between the "XDirection" and the "YDirection" after transformation.</description>
        </method>
        <method name="Rotated" qualifiers="const">
            <description>Rotates an axis placement. &lt;theA1&gt; is the axis of the rotation . theAng is the angular value of the rotation in radians.</description>
        </method>
        <method name="Scaled" qualifiers="const">
            <description>Applies a scaling transformation on the axis placement. The "Location" point of the axisplacement is modified. Warnings : If the scale &lt;theS&gt; is negative : . the main direction of the axis placement is not changed. . The "XDirection" and the "YDirection" are reversed. So the axis placement stay right handed.</description>
        </method>
        <method name="Transformed" qualifiers="const">
            <description>Transforms an axis placement with a Trsf. The "Location" point, the "XDirection" and the "YDirection" are transformed with theT. The resulting main "Direction" of &lt;me&gt; is the cross product between the "XDirection" and the "YDirection" after transformation.</description>
        </method>
        <method name="Translated_1" qualifiers="const">
            <description>Translates an axis plaxement in the direction of the vector &lt;theV&gt;. The magnitude of the translation is the vector's magnitude.</description>
        </method>
        <method name="Translated_2" qualifiers="const">
            <description>Translates an axis placement from the point &lt;theP1&gt; to the point &lt;theP2&gt;.</description>
        </method>
        <method name="DumpJson" qualifiers="const">
            <description>Dumps the content of me into the stream</description>
        </method>
    </methods>
</class>