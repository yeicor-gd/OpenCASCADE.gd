<?xml version='1.0' encoding='utf-8'?>
<class name="OcgBrepprimapiMakebox" inherits="RefCounted">
    <brief_description>Describes functions to build parallelepiped boxes. A MakeBox object provides a framework for: - defining the construction of a box, - implementing the construction algorithm, and - consulting the result. Constructs a box such that its sides are parallel to the axes of - the global coordinate system, or - the local coordinate system Axis. and - with a corner at (0, 0, 0) and of size (dx, dy, dz), or - with a corner at point P and of size (dx, dy, dz), or - with corners at points P1 and P2. Exceptions Standard_DomainError if: dx, dy, dz are less than or equal to Precision::Confusion(), or - the vector joining the points P1 and P2 has a component projected onto the global coordinate system less than or equal to Precision::Confusion(). In these cases, the box would be flat.</brief_description>
    <description>! Describes functions to build parallelepiped boxes.
! A MakeBox object provides a framework for:
! -   defining the construction of a box,
! -   implementing the construction algorithm, and
! -   consulting the result.
! Constructs a box such that its sides are parallel to the axes of
! -   the global coordinate system, or
! -   the local coordinate system Axis. and
! -   with a corner at (0, 0, 0) and of size (dx, dy, dz), or
! -   with a corner at point P and of size (dx, dy, dz), or
! -   with corners at points P1 and P2.
! Exceptions
! Standard_DomainError if: dx, dy, dz are less than or equal to
! Precision::Confusion(), or
! -   the vector joining the points P1 and P2 has a
! component projected onto the global coordinate
! system less than or equal to Precision::Confusion().
! In these cases, the box would be flat.</description>
    <methods>
        <method name="default" qualifiers="static">
            <description>Default constructor</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Make a box with corners P1,P2.</description>
        </method>
        <method name="from_3" qualifiers="static">
            <description>Make a box with a corner at 0,0,0 and the other dx,dy,dz</description>
        </method>
        <method name="from_4" qualifiers="static">
            <description>Make a box with a corner at P and size dx, dy, dz.</description>
        </method>
        <method name="from_5" qualifiers="static">
            <description>Make a box with Ax2 (the left corner and the axis) and size dx, dy, dz.</description>
        </method>
        <method name="Init_2">
            <description>Init a box with a corner at 0,0,0 and the other theDX, theDY, theDZ</description>
        </method>
        <method name="Init_3">
            <description>Init a box with a corner at thePnt and size theDX, theDY, theDZ.</description>
        </method>
        <method name="Init_1">
            <description>Init a box with corners thePnt1, thePnt2.</description>
        </method>
        <method name="Init_4">
            <description>Init a box with Ax2 (the left corner and the theAxes) and size theDX, theDY, theDZ.</description>
        </method>
        <method name="Build">
            <description>Stores the solid in myShape.</description>
        </method>
        <method name="Shell">
            <description>Returns the constructed box as a shell.</description>
        </method>
        <method name="Solid">
            <description>Returns the constructed box as a solid.</description>
        </method>
        <method name="BottomFace">
            <description>Returns ZMin face</description>
        </method>
        <method name="BackFace">
            <description>Returns XMin face</description>
        </method>
        <method name="FrontFace">
            <description>Returns XMax face</description>
        </method>
        <method name="LeftFace">
            <description>Returns YMin face</description>
        </method>
        <method name="RightFace">
            <description>Returns YMax face</description>
        </method>
        <method name="TopFace">
            <description>Returns ZMax face</description>
        </method>
    </methods>
</class>