<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTransferBinder" inherits="RefCounted">
    <brief_description>A Binder is an auxiliary object to Map the Result of the Transfer of a given Object : it records the Result of the Unitary Transfer (Resulting Object), status of progress and error (if any) of the Process</brief_description>
    <description>! A Binder is an auxiliary object to Map the Result of the
! Transfer of a given Object : it records the Result of the
! Unitary Transfer (Resulting Object), status of progress and
! error (if any) of the Process
!
! The class Binder itself makes no definition for the Result :
! it is defined by sub-classes : it can be either Simple (and
! has to be typed : see generic class SimpleBinder) or Multiple
! (see class MultipleBinder).
!
! In principle, for a Transfer in progress, Result cannot be
! accessed : this would cause an exception raising.
! This is controlled by the value if StatusResult : if it is
! "Used", the Result cannot be changed. This status is normally
! controlled by TransferProcess but can be directly (see method
! SetAlreadyUsed)
!
! Checks can be completed by a record of cases, as string which
! can be used as codes, but not to be printed
!
! In addition to the Result, a Binder can bring a list of
! Attributes, which are additional data, each of them has a name</description>
    <methods>
        <method name="Merge">
            <description>Merges basic data (Check, ExecStatus) from another Binder but keeps its result. Used when a binder is replaced by another one, this allows to keep messages</description>
        </method>
        <method name="IsMultiple" qualifiers="const">
            <description>Returns True if a Binder has several results, either by itself or because it has next results Can be defined by sub-classes.</description>
        </method>
        <method name="AddResult">
            <description>Adds a next result (at the end of the list) Remark : this information is not processed by Merge</description>
        </method>
        <method name="NextResult" qualifiers="const">
            <description>Returns the next result, Null if none</description>
        </method>
        <method name="HasResult" qualifiers="const">
            <description>Returns True if a Result is available (StatusResult = Defined) A Unique Result will be gotten by Result (which must be defined in each sub-class according to result type) For a Multiple Result, see class MultipleBinder For other case, specific access has to be forecast</description>
        </method>
        <method name="SetAlreadyUsed">
            <description>Declares that result is now used by another one, it means that it cannot be modified (by Rebind)</description>
        </method>
        <method name="Status" qualifiers="const">
            <description>Returns status, which can be Initial (not yet done), Made (a result is recorded, not yet shared), Used (it is shared and cannot be modified)</description>
        </method>
        <method name="StatusExec" qualifiers="const">
            <description>Returns execution status</description>
        </method>
        <method name="SetStatusExec">
            <description>Modifies execution status; called by TransferProcess only (for StatusError, rather use SetError, below)</description>
        </method>
        <method name="AddFail">
            <description>Used to declare an individual transfer as being erroneous (Status is set to Void, StatusExec is set to Error, &lt;errmess&gt; is added to Check's list of Fails) It is possible to record several messages of error</description>
        </method>
        <method name="AddWarning">
            <description>Used to attach a Warning Message to an individual Transfer It has no effect on the Status</description>
        </method>
        <method name="Check" qualifiers="const">
            <description>Returns Check which stores Fail messages Note that no Entity is associated in this Check</description>
        </method>
        <method name="CCheck">
            <description>Returns Check which stores Fail messages, in order to modify it (adding messages, or replacing it)</description>
        </method>
    </methods>
</class>