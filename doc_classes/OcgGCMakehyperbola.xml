<?xml version='1.0' encoding='utf-8'?>
<class name="OcgGCMakehyperbola" inherits="RefCounted">
    <brief_description>This class implements construction algorithms for a hyperbola in 3D space. The result is a Geom_Hyperbola hyperbola. A MakeHyperbola object provides a framework for: - defining the construction of the hyperbola, - implementing the construction algorithm, and - consulting the results. In particular, the Value function returns the constructed hyperbola. To define the main branch of an hyperbola. The parameterization range is ]-infinite,+infinite[ It is possible to get the other branch and the two conjugate branches of the main branch.</brief_description>
    <description>! This class implements construction algorithms for a hyperbola in
! 3D space. The result is a Geom_Hyperbola hyperbola.
! A MakeHyperbola object provides a framework for:
! -   defining the construction of the hyperbola,
! -   implementing the construction algorithm, and
! -   consulting the results. In particular, the Value
! function returns the constructed hyperbola.
! To define the main branch of an hyperbola.
! The parameterization range is ]-infinite,+infinite[
! It is possible to get the other branch and the two conjugate
! branches of the main branch.
!
! ^YAxis
! |
! FirstConjugateBranch
! |
! Other            |                Main
! --------------------- C ------------------------------&gt;XAxis
! Branch           |                Branch
! |
! SecondConjugateBranch
! |
!
! The major radius is the distance between the Location point
! of the hyperbola C and the apex of the Main Branch (or the
! Other branch). The major axis is the XAxis.
! The minor radius is the distance between the Location point
! of the hyperbola C and the apex of the First (or Second)
! Conjugate branch. The minor axis is the YAxis.
! The major radius can be lower than the minor radius.</description>
    <methods>
        <method name="from_1" qualifiers="static">
            <description>Creates an Hyperbola from a non persistent hyperbola from package gp by conversion.</description>
        </method>
        <method name="from_2" qualifiers="static">
            <description>Constructs a hyperbola centered on the origin of the coordinate system A2, with major and minor radii MajorRadius and MinorRadius, where: the plane of the hyperbola is defined by the "X Axis" and "Y Axis" of A2, - its major axis is the "X Axis" of A2.</description>
        </method>
        <method name="from_3" qualifiers="static">
            <description>Constructs a hyperbola centered on the point Center, where - the plane of the hyperbola is defined by Center, S1 and S2, - its major axis is defined by Center and S1, - its major radius is the distance between Center and S1, and - its minor radius is the distance between S2 and the major axis;</description>
        </method>
        <method name="Value" qualifiers="const">
            <description>Returns the constructed hyperbola. Exceptions StdFail_NotDone if no hyperbola is constructed.</description>
        </method>
    </methods>
</class>