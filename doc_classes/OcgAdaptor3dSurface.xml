<?xml version='1.0' encoding='utf-8'?>
<class name="OcgAdaptor3dSurface" inherits="RefCounted">
    <brief_description>Root class for surfaces on which geometric algorithms work. An adapted surface is an interface between the services provided by a surface and those required of the surface by algorithms which use it. A derived concrete class is provided: GeomAdaptor_Surface for a surface from the Geom package. The Surface class describes the standard behaviour of a surface for generic algorithms.</brief_description>
    <description>! Root class for surfaces on which geometric algorithms work.
! An adapted surface is an interface between the
! services provided by a surface and those required of
! the surface by algorithms which use it.
! A derived concrete class is provided:
! GeomAdaptor_Surface for a surface from the Geom package.
! The  Surface class describes  the standard behaviour
! of a surface for generic algorithms.
!
! The Surface can  be decomposed in intervals of any
! continuity in U and V using the method NbIntervals.
! A current interval can be set.
! Most of the methods apply to the current interval.
! Warning: All the methods are virtual and implemented with a
! raise to allow to redefined only the methods really used.
!
! Polynomial coefficients of BSpline surfaces used for their evaluation are cached for better
! performance. Therefore these evaluations are not thread-safe and parallel evaluations need to be
! prevented.</description>
    <methods>
        <method name="NbUIntervals" qualifiers="const">
            <description>Returns the number of U intervals for continuity &lt;S&gt;. May be one if UContinuity(me) &gt;= &lt;S&gt;</description>
        </method>
        <method name="NbVIntervals" qualifiers="const">
            <description>Returns the number of V intervals for continuity &lt;S&gt;. May be one if VContinuity(me) &gt;= &lt;S&gt;</description>
        </method>
        <method name="UIntervals" qualifiers="const">
            <description>Returns the intervals with the requested continuity in the U direction.</description>
        </method>
        <method name="VIntervals" qualifiers="const">
            <description>Returns the intervals with the requested continuity in the V direction.</description>
        </method>
        <method name="UTrim" qualifiers="const">
            <description>Returns a surface trimmed in the U direction equivalent of &lt;me&gt; between parameters &lt;First&gt; and &lt;Last&gt;. &lt;Tol&gt; is used to test for 3d points confusion. If &lt;First&gt; &gt;= &lt;Last&gt;</description>
        </method>
        <method name="VTrim" qualifiers="const">
            <description>Returns a surface trimmed in the V direction between parameters &lt;First&gt; and &lt;Last&gt;. &lt;Tol&gt; is used to test for 3d points confusion. If &lt;First&gt; &gt;= &lt;Last&gt;</description>
        </method>
        <method name="Value" qualifiers="const">
            <description>Computes the point of parameters U,V on the surface. Tip: use GeomLib::NormEstim() to calculate surface normal at specified (U, V) point.</description>
        </method>
        <method name="D0" qualifiers="const">
            <description>Computes the point of parameters U,V on the surface.</description>
        </method>
        <method name="D1" qualifiers="const">
            <description>Computes the point and the first derivatives on the surface. Raised if the continuity of the current intervals is not C1.</description>
        </method>
        <method name="D2" qualifiers="const">
            <description>Computes the point, the first and second derivatives on the surface. Raised if the continuity of the current intervals is not C2.</description>
        </method>
        <method name="D3" qualifiers="const">
            <description>Computes the point, the first, second and third derivatives on the surface. Raised if the continuity of the current intervals is not C3.</description>
        </method>
        <method name="DN" qualifiers="const">
            <description>Computes the derivative of order Nu in the direction U and Nv in the direction V at the point P(U, V). Raised if the current U interval is not not CNu and the current V interval is not CNv. Raised if Nu + Nv &lt; 1 or Nu &lt; 0 or Nv &lt; 0.</description>
        </method>
        <method name="UResolution" qualifiers="const">
            <description>Returns the parametric U resolution corresponding to the real space resolution &lt;R3d&gt;.</description>
        </method>
        <method name="VResolution" qualifiers="const">
            <description>Returns the parametric V resolution corresponding to the real space resolution &lt;R3d&gt;.</description>
        </method>
        <method name="GetType" qualifiers="const">
            <description>Returns the type of the surface : Plane, Cylinder, Cone, Sphere, Torus, BezierSurface, BSplineSurface, SurfaceOfRevolution, SurfaceOfExtrusion, OtherSurface</description>
        </method>
    </methods>
</class>