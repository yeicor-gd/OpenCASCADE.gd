<?xml version='1.0' encoding='utf-8'?>
<class name="OcgAdaptor3dInterfunc" inherits="RefCounted">
    <brief_description>Used to find the points U(t) = U0 or V(t) = V0 in order to determine the Cn discontinuities of an Adpator_CurveOnSurface relatively to the discontinuities of the surface. Used to find the roots of the functions</brief_description>
    <description>! Used to find the points U(t) = U0 or V(t) = V0 in
! order to determine the  Cn discontinuities of  an
! Adpator_CurveOnSurface  relatively  to    the
! discontinuities of the surface. Used to
! find the roots of the functions</description>
    <methods>
        <method name="Adaptor3d_InterFunc" qualifiers="static">
            <description>build the function U(t)=FixVal if Fix =1 or V(t)=FixVal if Fix=2</description>
        </method>
        <method name="Value">
            <description>computes the value &lt;F&gt;of the function for the variable &lt;X&gt;. Returns True if the calculation were successfully done, False otherwise.</description>
        </method>
        <method name="Derivative">
            <description>computes the derivative &lt;D&gt; of the function for the variable &lt;X&gt;. Returns True if the calculation were successfully done, False otherwise.</description>
        </method>
        <method name="Values">
            <description>computes the value &lt;F&gt; and the derivative &lt;D&gt; of the function for the variable &lt;X&gt;. Returns True if the calculation were successfully done, False otherwise.</description>
        </method>
    </methods>
</class>