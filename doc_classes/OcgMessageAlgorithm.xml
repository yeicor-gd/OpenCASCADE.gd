<?xml version='1.0' encoding='utf-8'?>
<class name="OcgMessageAlgorithm" inherits="RefCounted">
    <brief_description>Class Message_Algorithm is intended to be the base class for classes implementing algorithms or any operations that need to provide extended information on its execution to the caller / user.</brief_description>
    <description>! Class Message_Algorithm is intended to be the base class for
! classes implementing algorithms or any operations that need
! to provide extended information on its execution to the
! caller / user.
!
! It provides generic mechanism for management of the execution
! status, collection and output of messages.
!
! The algorithm uses methods SetStatus() to set an execution status.
! It is possible to associate a status with a number or a string
! (second argument of SetStatus() methods) to indicate precisely
! the item (object, element etc.) in the input data which caused
! the problem.
!
! Each execution status generated by the algorithm has associated
! text message that should be defined in the resource file loaded
! with call to Message_MsgFile::LoadFile().
!
! The messages corresponding to the statuses generated during the
! algorithm execution are output to Message_Messenger using
! methods SendMessages(). If status have associated numbers
! or strings, they are included in the message body in place of
! "%s" placeholder which should be present in the message text.
!
! The name of the message text in the resource file is constructed
! from name of the class and name of the status, separated by dot,
! for instance:
!
! .TObj_CheckModel.Alarm2
! Error: Some objects (%s) have references to dead object(s)
!
! If message for the status is not found with prefix of
! the current class type, the same message is searched for the base
! class(es) recursively.
!
! Message can be set explicitly for the status; in this case the
! above procedure is not used and supplied message is used as is.
!
! The messages are output to the messenger, stored in the field;
! though messenger can be changed, it is guaranteed to be non-null.
! By default, Message::DefaultMessenger() is used.</description>
    <methods>
        <method name="Message_Algorithm" qualifiers="static">
            <description>Empty constructor</description>
        </method>
        <method name="SetStatus_1">
            <description>Sets status with no parameter</description>
        </method>
        <method name="SetStatus_2">
            <description>Sets status with integer parameter</description>
        </method>
        <method name="SetStatus_4">
            <description>Sets status with string parameter. If noRepetitions is True, the parameter will be added only if it has not been yet recorded for the same status flag</description>
        </method>
        <method name="SetStatus_6">
            <description>Sets status with string parameter If noRepetitions is True, the parameter will be added only if it has not been yet recorded for the same status flag</description>
        </method>
        <method name="SetStatus_8">
            <description>Sets status with string parameter If noRepetitions is True, the parameter will be added only if it has not been yet recorded for the same status flag</description>
        </method>
        <method name="SetStatus_3">
            <description>Sets status with preformatted message. This message will be used directly to report the status; automatic generation of status messages will be disabled for it.</description>
        </method>
        <method name="GetStatus" qualifiers="const">
            <description>Returns copy of exec status of algorithm</description>
        </method>
        <method name="ChangeStatus">
            <description>Returns exec status of algorithm</description>
        </method>
        <method name="ClearStatus">
            <description>Clear exec status of algorithm</description>
        </method>
        <method name="SetMessenger">
            <description>Sets messenger to algorithm</description>
        </method>
        <method name="GetMessenger" qualifiers="const">
            <description>Returns messenger of algorithm. The returned handle is always non-null and can be used for sending messages.</description>
        </method>
        <method name="SendStatusMessages" qualifiers="const">
            <description>Print messages for all status flags that have been set during algorithm execution, excluding statuses that are NOT set in theFilter.</description>
        </method>
        <method name="SendMessages" qualifiers="const">
            <description>Convenient variant of SendStatusMessages() with theFilter having defined all WARN, ALARM, and FAIL (but not DONE) status flags</description>
        </method>
        <method name="AddStatus_1">
            <description>Add statuses to this algorithm from other algorithm (including messages)</description>
        </method>
        <method name="AddStatus_2">
            <description>Add statuses to this algorithm from other algorithm, but only those items are moved that correspond to statuses set in theStatus</description>
        </method>
        <method name="GetMessageNumbers" qualifiers="const">
            <description>Return the numbers associated with the indicated status; Null handle if no such status or no numbers associated with it</description>
        </method>
        <method name="GetMessageStrings" qualifiers="const">
            <description>Return the strings associated with the indicated status; Null handle if no such status or no strings associated with it</description>
        </method>
    </methods>
</class>