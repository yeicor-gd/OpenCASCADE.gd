<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTransferTransferdispatch" inherits="RefCounted">
    <brief_description>A TransferDispatch is aimed to dispatch Entities between two Interface Models, by default by copying them, as CopyTool, but with more capabilities of adapting : Copy is redefined to firstly pass the hand to a TransferProcess. If this gives no result, standard Copy is called.</brief_description>
    <description>! A TransferDispatch is aimed to dispatch Entities between two
! Interface Models, by default by copying them, as CopyTool, but
! with more capabilities of adapting : Copy is redefined to
! firstly pass the hand to a TransferProcess. If this gives no
! result, standard Copy is called.
!
! This allow, for instance, to modify the copied Entity (such as
! changing a Name for a VDA Entity), or to do a deeper work
! (such as Substituting a kind of Entity to another one).
!
! For these reasons, TransferDispatch is basically a CopyTool,
! but uses a more sophiscated control, which is TransferProcess,
! and its method Copy is redefined</description>
    <methods>
        <method name="from_1" qualifiers="static">
            <description>Same as above, but works with the Active Protocol</description>
        </method>
        <method name="from_3" qualifiers="static">
            <description>Same as above, but Library is defined through a Protocol</description>
        </method>
        <method name="TransientProcess" qualifiers="const">
            <description>Returns the content of Control Object, as a TransientProcess</description>
        </method>
        <method name="Copy">
            <description>Copies an Entity by calling the method Transferring from the TransferProcess. If this called produces a Null Binder, then the standard, inherited Copy is called</description>
        </method>
    </methods>
</class>