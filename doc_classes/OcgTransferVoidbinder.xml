<?xml version='1.0' encoding='utf-8'?>
<class name="OcgTransferVoidbinder" inherits="RefCounted">
    <brief_description>a VoidBinder is used to bind a starting item with a status, error or warning messages, but no result It is interpreted by TransferProcess, which admits a VoidBinder to be over-written, and copies its check to the new Binder</brief_description>
    <description>! a VoidBinder is used to bind a starting item with a status,
! error or warning messages, but no result
! It is interpreted by TransferProcess, which admits a
! VoidBinder to be over-written, and copies its check to the
! new Binder</description>
    <methods>
        <method name="Transfer_VoidBinder" qualifiers="static">
            <description>a VoidBinder is not Multiple (Remark : it is not Simple too) But it can bring next results ...</description>
        </method>
        <method name="ResultTypeName" qualifiers="const">
            <description>Returns "(void)"</description>
        </method>
    </methods>
</class>